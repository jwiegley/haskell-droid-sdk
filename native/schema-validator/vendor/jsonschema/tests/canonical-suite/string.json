[
  {
    "description": "ir: string length window preserves validation",
    "schema": {"type": "string", "minLength": 2, "maxLength": 4},
    "expected": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "string",
      "minLength": 2,
      "maxLength": 4
    },
    "tests": [
      {"data": "ab", "valid": true},
      {"data": "abcd", "valid": true},
      {"data": "a", "valid": false},
      {"data": "abcde", "valid": false},
      {"data": 5, "valid": false}
    ]
  },
  {
    "description": "ir: minLength zero converges to the bare type",
    "schemas": [
      {"type": "string", "minLength": 0},
      {"type": "string"}
    ],
    "expected": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "string"
    }
  },
  {
    "description": "ir: bare pattern constrains only strings",
    "schema": {"pattern": "^a"},
    "expected": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "anyOf": [
        {"type": ["null", "boolean", "number", "array", "object"]},
        {"type": "string", "pattern": "^a"}
      ]
    },
    "tests": [
      {"data": "abc", "valid": true},
      {"data": "xyz", "valid": false},
      {"data": 5, "valid": true},
      {"data": null, "valid": true}
    ]
  },
  {
    "description": "ir: allOf of string leaves tightens to one leaf",
    "schema": {
      "allOf": [
        {"type": "string", "minLength": 2},
        {"type": "string", "minLength": 3, "maxLength": 5}
      ]
    },
    "expected": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "string",
      "minLength": 3,
      "maxLength": 5
    },
    "tests": [
      {"data": "abc", "valid": true},
      {"data": "abcde", "valid": true},
      {"data": "ab", "valid": false},
      {"data": "abcdef", "valid": false}
    ]
  },
  {
    "description": "ir: multiple patterns emit as allOf and every pattern applies",
    "schema": {
      "allOf": [
        {"type": "string", "pattern": "^a"},
        {"type": "string", "pattern": "z$"}
      ]
    },
    "expected": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "string",
      "allOf": [{"pattern": "^a"}, {"pattern": "z$"}]
    },
    "tests": [
      {"data": "az", "valid": true},
      {"data": "abz", "valid": true},
      {"data": "ab", "valid": false},
      {"data": "bz", "valid": false}
    ]
  },
  {
    "description": "ir: constant string filtered by a pattern",
    "schema": {"const": "abc", "pattern": "^a"},
    "expected": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "const": "abc"
    },
    "tests": [
      {"data": "abc", "valid": true},
      {"data": "abd", "valid": false}
    ]
  },
  {
    "description": "ir: constant string excluded by a pattern is unsatisfiable",
    "schema": {"const": "xyz", "pattern": "^a"},
    "satisfiable": false,
    "tests": [
      {"data": "xyz", "valid": false}
    ],
    "expected": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "not": {}
    }
  },
  {
    "description": "ir: empty string length window is unsatisfiable",
    "schema": {"type": "string", "minLength": 5, "maxLength": 2},
    "satisfiable": false,
    "tests": [
      {"data": "abc", "valid": false}
    ],
    "expected": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "not": {}
    }
  },
  {
    "description": "ir: integer-valued float length bound is modeled like the validator",
    "schema": {"type": "string", "minLength": 3.0},
    "expected": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "string",
      "minLength": 3
    },
    "tests": [
      {"data": "abc", "valid": true},
      {"data": "ab", "valid": false}
    ]
  },
  {
    "description": "ir: minLength alone emits a single bound",
    "schema": {"type": "string", "minLength": 3},
    "expected": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "string",
      "minLength": 3
    },
    "tests": [
      {"data": "abc", "valid": true},
      {"data": "ab", "valid": false}
    ]
  },
  {
    "description": "ir: maxLength alone emits a single bound",
    "schema": {"type": "string", "maxLength": 3},
    "expected": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "string",
      "maxLength": 3
    },
    "tests": [
      {"data": "abc", "valid": true},
      {"data": "abcd", "valid": false}
    ]
  },
  {
    "description": "ir: typed string pattern emits back",
    "schema": {"type": "string", "pattern": "^a"},
    "expected": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "string",
      "pattern": "^a"
    },
    "tests": [
      {"data": "abc", "valid": true},
      {"data": "xyz", "valid": false},
      {"data": 5, "valid": false}
    ]
  },
  {
    "description": "ir: bare length facet constrains only strings",
    "schema": {"minLength": 3},
    "expected": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "anyOf": [
        {"type": ["null", "boolean", "number", "array", "object"]},
        {"type": "string", "minLength": 3}
      ]
    },
    "tests": [
      {"data": "abc", "valid": true},
      {"data": "ab", "valid": false},
      {"data": 5, "valid": true}
    ]
  },
  {
    "description": "ir: impossible bare length window keeps only non-strings",
    "schema": {"minLength": 5, "maxLength": 2},
    "expected": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": ["null", "boolean", "number", "array", "object"]
    },
    "tests": [
      {"data": "abc", "valid": false},
      {"data": 5, "valid": true}
    ]
  },
  {
    "description": "ir: bare minLength zero is the always-true schema",
    "schema": {"minLength": 0},
    "expected": {"$schema": "https://json-schema.org/draft/2020-12/schema"},
    "tests": [
      {"data": "anything", "valid": true},
      {"data": 5, "valid": true}
    ]
  },
  {
    "description": "ir: constant string within its length window is kept",
    "schema": {"const": "abc", "minLength": 2},
    "expected": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "const": "abc"
    },
    "tests": [
      {"data": "abc", "valid": true},
      {"data": "ab", "valid": false}
    ]
  },
  {
    "description": "ir: constant string below its minimum length is unsatisfiable",
    "schema": {"const": "a", "minLength": 2},
    "satisfiable": false,
    "tests": [
      {"data": "a", "valid": false}
    ],
    "expected": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "not": {}
    }
  },
  {
    "description": "ir: non-overlapping string windows stay separate anyOf branches",
    "schema": {
      "anyOf": [
        {"type": "string", "maxLength": 2},
        {"type": "string", "minLength": 5}
      ]
    },
    "expected": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "anyOf": [
        {"type": "string", "maxLength": 2},
        {"type": "string", "minLength": 5}
      ]
    },
    "tests": [
      {"data": "a", "valid": true},
      {"data": "abcde", "valid": true},
      {"data": "abc", "valid": false}
    ]
  },
  {
    "description": "ir: two maxLength bounds tighten to the smaller",
    "schema": {
      "allOf": [
        {"type": "string", "maxLength": 5},
        {"type": "string", "maxLength": 3}
      ]
    },
    "expected": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "string",
      "maxLength": 3
    },
    "tests": [
      {"data": "abc", "valid": true},
      {"data": "abcd", "valid": false}
    ]
  },
  {
    "description": "ir: a length facet skips non-string enum members",
    "schema": {"enum": [1, "abc"], "minLength": 2},
    "expected": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "enum": [1, "abc"]
    },
    "tests": [
      {"data": 1, "valid": true},
      {"data": "abc", "valid": true},
      {"data": "a", "valid": false},
      {"data": 2, "valid": false}
    ]
  },
  {
    "description": "ir: a constant string longer than maxLength is unsatisfiable",
    "schema": {"const": "abcd", "maxLength": 3},
    "satisfiable": false,
    "tests": [
      {"data": "abcd", "valid": false}
    ],
    "expected": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "not": {}
    }
  },
  {
    "description": "ir: invalid regex in pattern is rejected",
    "schema": {"pattern": "["},
    "error": "InvalidPattern"
  },
  {
    "description": "ir: format is a string facet",
    "schema": {"type": "string", "format": "email"},
    "validate_formats": true,
    "expected": {"$schema": "https://json-schema.org/draft/2020-12/schema", "type": "string", "format": "email"},
    "tests": [
      {"data": "a@b.com", "valid": true},
      {"data": "x", "valid": false},
      {"data": 1, "valid": false}
    ]
  },
  {
    "description": "ir: format survives beside a length window and a pattern",
    "schema": {"type": "string", "format": "email", "minLength": 5, "pattern": "^a"},
    "validate_formats": true,
    "expected": {"$schema": "https://json-schema.org/draft/2020-12/schema", "type": "string", "format": "email", "minLength": 5, "pattern": "^a"},
    "tests": [
      {"data": "aa@b.com", "valid": true},
      {"data": "b@b.com", "valid": false}
    ]
  },
  {
    "description": "ir: the same format on both sides of an allOf is kept once",
    "schema": {"allOf": [{"type": "string", "format": "date"}, {"type": "string", "format": "date"}]},
    "validate_formats": true,
    "expected": {"$schema": "https://json-schema.org/draft/2020-12/schema", "type": "string", "format": "date"},
    "tests": [
      {"data": "2020-01-01", "valid": true},
      {"data": "nope", "valid": false}
    ]
  },
  {
    "description": "ir: an annotation-only format is dropped from the canonical form",
    "schema": {"type": "string", "format": "email"},
    "validate_formats": false,
    "expected": {"$schema": "https://json-schema.org/draft/2020-12/schema", "type": "string"},
    "tests": [
      {"data": "not-an-email", "valid": true}
    ]
  },
  {
    "description": "ir: two formats that both must hold are emitted as an allOf",
    "schema": {"allOf": [{"type": "string", "format": "email"}, {"type": "string", "format": "idn-email"}]},
    "validate_formats": true,
    "expected": {"$schema": "https://json-schema.org/draft/2020-12/schema", "type": "string", "allOf": [{"format": "email"}, {"format": "idn-email"}]},
    "tests": [
      {"data": "a@b.com", "valid": true},
      {"data": "x", "valid": false}
    ]
  },
  {
    "description": "ir: a length window too short for the format admits nothing",
    "schema": {"type": "string", "format": "ipv4", "maxLength": 5},
    "validate_formats": true,
    "expected": {"$schema": "https://json-schema.org/draft/2020-12/schema", "not": {}},
    "satisfiable": false
  },
  {
    "description": "ir: formats whose lengths can overlap are not proven disjoint - date and ipv4",
    "schema": {"allOf": [{"type": "string", "format": "date"}, {"type": "string", "format": "ipv4"}]},
    "validate_formats": true,
    "satisfiable": true,
    "expected": {"$schema": "https://json-schema.org/draft/2020-12/schema", "type": "string", "allOf": [{"format": "date"}, {"format": "ipv4"}]}
  },
  {
    "description": "ir: a value is not absorbed by a leaf whose format the draft cannot check",
    "schema": {"anyOf": [{"type": "string", "format": "my-fmt"}, {"const": "xyz"}]},
    "validate_formats": true,
    "expected": {"$schema": "https://json-schema.org/draft/2020-12/schema", "anyOf": [{"format": "my-fmt", "type": "string"}, {"const": "xyz"}]},
    "tests": [
      {"data": "xyz", "valid": true}
    ]
  },
  {
    "description": "ir: an unrecognized format leaves every value of an intersecting set in place",
    "schema": {"allOf": [{"type": "string", "format": "my-fmt"}, {"enum": ["a", "b"]}]},
    "validate_formats": true,
    "expected": {"$schema": "https://json-schema.org/draft/2020-12/schema", "enum": ["a", "b"]},
    "tests": [
      {"data": "a", "valid": true},
      {"data": "b", "valid": true},
      {"data": "c", "valid": false}
    ]
  },
  {
    "description": "ir: excluding the empty string is the length floor above it",
    "schemas": [
      {"type": "string", "not": {"enum": [""]}},
      {"type": "string", "minLength": 1}
    ],
    "expected": {"$schema": "https://json-schema.org/draft/2020-12/schema", "type": "string", "minLength": 1},
    "tests": [
      {"data": "", "valid": false},
      {"data": "a", "valid": true},
      {"data": 1, "valid": false}
    ]
  },
  {
    "description": "ir: excluding the empty string under a ceiling keeps the ceiling",
    "schemas": [
      {"type": "string", "maxLength": 3, "not": {"enum": [""]}},
      {"type": "string", "minLength": 1, "maxLength": 3}
    ],
    "expected": {"$schema": "https://json-schema.org/draft/2020-12/schema", "type": "string", "minLength": 1, "maxLength": 3},
    "tests": [
      {"data": "", "valid": false},
      {"data": "abc", "valid": true},
      {"data": "abcd", "valid": false}
    ]
  },
  {
    "description": "ir: an exclusion above the floor stays an exclusion",
    "schema": {"type": "string", "not": {"enum": ["a"]}},
    "expected": {"$schema": "https://json-schema.org/draft/2020-12/schema", "type": "string", "not": {"enum": ["a"]}},
    "tests": [
      {"data": "", "valid": true},
      {"data": "a", "valid": false},
      {"data": "b", "valid": true}
    ]
  },
  {
    "description": "ir: intersecting string leaves can produce an empty length window",
    "schema": {
      "allOf": [
        {"type": "string", "minLength": 5},
        {"type": "string", "maxLength": 2}
      ]
    },
    "expected": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "not": {}
    },
    "satisfiable": false,
    "tests": [
      {"data": "a", "valid": false},
      {"data": "abcdef", "valid": false}
    ]
  },
  {
    "description": "not a pattern bars it on the string branch and admits every other type",
    "schemas": [
      {"not": {"type": "string", "pattern": "^a"}}
    ],
    "expected": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "anyOf": [
        {"type": ["null", "boolean", "number", "array", "object"]},
        {"type": "string", "allOf": [{"not": {"pattern": "^a"}}]}
      ]
    },
    "tests": [
      {"data": "abc", "valid": false},
      {"data": "xyz", "valid": true},
      {"data": 1, "valid": true},
      {"data": null, "valid": true}
    ]
  },
  {
    "description": "maxLength 0 leaves the empty string, which the pattern beside it rejects",
    "schemas": [
      {"type": "string", "maxLength": 0, "pattern": "^a"},
      {"allOf": [{"type": "string", "maxLength": 0}, {"pattern": "^a"}]}
    ],
    "expected": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "not": {}
    },
    "satisfiable": false,
    "tests": [
      {"data": "", "valid": false},
      {"data": "a", "valid": false}
    ]
  },
  {
    "description": "maxLength 0 beside a pattern the empty string matches accepts that one string",
    "schemas": [
      {"type": "string", "maxLength": 0, "pattern": "^$"},
      {"const": ""}
    ],
    "expected": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "const": ""
    },
    "tests": [
      {"data": "", "valid": true},
      {"data": "a", "valid": false},
      {"data": null, "valid": false}
    ]
  },
  {
    "description": "maxLength 0 beside a format the empty string does not satisfy",
    "schema": {"type": "string", "maxLength": 0, "format": "email"},
    "validate_formats": true,
    "expected": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "not": {}
    },
    "satisfiable": false,
    "tests": [
      {"data": "", "valid": false},
      {"data": "a@b.com", "valid": false}
    ]
  },
  {
    "description": "a branch accepting only the empty string joins the enum beside it",
    "schema": {
      "anyOf": [
        {"type": "string", "maxLength": 0, "pattern": "^[ab]+$"},
        {"enum": ["", null]}
      ]
    },
    "expected": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "enum": [null, ""]
    },
    "tests": [
      {"data": "", "valid": true},
      {"data": null, "valid": true},
      {"data": "a", "valid": false}
    ]
  }
]
