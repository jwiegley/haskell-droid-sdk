# Droid SDK Haskell: delivery handoff

<!-- handoff-id: droid-sdk-haskell-2026-09-08; schema-version: 1 -->

Updated: 2026-09-14 after live daemon acceptance, the reproduced saved-session observer repair, full macOS/Linux suites, final source-distribution verification and required issue/task closure. Consult the native goal state for its independent completion-audit/lifecycle result. HEAD `6172dc8` is unchanged; work remains uncommitted and unpublished.

## Refocus state

- Active skill: `/Users/johnw/src/nix/config/ai/skills/refocus/SKILL.md`. Re-read on resumption or compaction; check the clock at work boundaries and around long calls.
- **User execution correction (September 11): keep the goal fixed and finish the known Python/TypeScript functional target. No repeated inventory, goal revision, automatic per-helper reviews or long report cycles. Implement coherent batches; focused checks while coding, one broad integration boundary.**
- **Standing approval and resumption (September14,05:35:07):** user authorized all future approval requests except critical decisions needing their input, and directed autonomous completion without repeated permission stops. Routine bounded live checks and required local setup are approved; record scope/results, preserve safety and do not waive failed checks. Earlier per-attempt authorization waits below are historical, not current gates. Escalate only critical decisions or genuinely unavailable required input.
- **Accepted release work:** all45/45 goal tasks and all required Obr issues, including audit correction hsdk-xrr, are complete/closed; original five completed milestone records are preserved. Corrected archive and verification receipts are under ~/Products/droid-sdk-haskell/audit-docs-20260914. Only explicitly excluded hsdk-vtk and hsdk-org-export-whitespace-x4c remain nonclosed. Goal-level re-audit outcome is recorded separately by the native mechanism; no new SDK authorization or credential input is needed.
- **Independent audit correction:** the first completion audit rejected stale README GHC9.10.3 requirements, unfinished/offline-only opening, Linux timestamp execution claims and in-progress Cabal description. The prior no-drift claim was too broad. hsdk-xrr corrects those plus related mission/attribution prose; the targeted checker records10 failures before and0 after in checkout/unpacking. Only README/parity prose and Cabal description changed; no runtime capability was reopened. The rejected archive and audit remain preserved.
- **User correction (September14,15:21):** the supplied key is active for SDK use, not direct Factory REST API use. Withdraw the earlier inference that a doctor/REST401 establishes invalidity for SDK authentication. The credential is available and may be tested through the reference SDK mechanism; no replacement-key wait or unchanged-file retry prohibition applies to this corrected investigation. Preserve the earlier negative receipts as observations of those paths.
- **Historical configuration wait (16:44:36):** automatic continuation stopped after three administrative checkpoints, not three new diagnostic experiments. The later claim that further progress required user configuration was stronger than the evidence. At 19:13 the user identified Pi Coding Agent's Droid integration as the working comparison; investigate that mechanism, not credential replacement.
- **Pi comparison (19:13–19:36):** an in-memory constant-time comparison confirms Pi's saved literal Factory credential exactly matches the supplied file. No current FACTORY_API_KEY environment override exists. Located the managed pi-droid-sdk package through Nix's CLI referrers: xrzjmmm2bsbj351rwklhxai6qwfpw1mv-pi-droid-sdk-0.1.0. It uses SDK 0.9.0, pinned CLI 0.218.1, explicit apiKey, and buildDroidProcessEnv; its bundled Node SDK omits implicit process.env inheritance. Source comparison is not proof of which package a separate working Pi session loaded.
- **One managed-SDK probe:** actual bundled SDK 0.9.0 plus the installed Pi environment helper, CLI 0.218.1 and model gpt-5.6-sol still returned a classified 401 in 6.26s. No raw output or authentication payload persisted; receipt contains fixed markers/counts only. SDK scope exited without forced cleanup; two sampled children present immediately afterward were absent on subsequent inspection. Preserve ~/Products/droid-sdk-haskell/pi-sdk-compare-20260914; do not reuse its attempt marker. No Haskell source change, key-invalidity conclusion, or daemon-success claim follows.
- **New key supplied (21:18):** user generated a new key and requested another check. Byte-identical Pi-managed SDK0.9.0/CLI0.218.1 scripts now return SDK_SUCCESS in11.49s, without stderr, unexpected stdout, forced cleanup or remaining sampled child. Sources/consumed marker/receipt: ~/Products/droid-sdk-haskell/pi-sdk-new-key-20260914. The previous Pi credential comparison concerned the old file contents; no Pi credential store was changed.
- **Native daemon live accepted:** existing DaemonSmoke.hs and run-smoke.py, pinned CLI0.217.0, distinct new-key attempt, passed all seven markers in32.32s: exact HELLO/recall stream-result-session equality, TurnCancelled, closed-query rejection, borrowed-connection reuse, authenticated reconnect/settings and scope completion. Probe/daemon exit0; no stderr, unexpected stdout, forced kill or remaining sampled descendants. Receipts: ~/Products/droid-sdk-haskell/live-daemon-20260914/new-key-{scope,result}.json. This resolves the authentication verification blocker without a Haskell change, false branding or auth bypass; the earlier working-model/configuration question is no longer needed.
- **Current SDK source/result:** Python main remains6d06b4e/0.4.0; actual venv imports that checkout. Explicitly requested current TypeScript comparison uses integrity-verified0.9.1, not a functional-baseline upgrade. Both local SDKs pass api_key/apiKey through FACTORY_API_KEY and add their own SDK attribution;0.9.1 daemon auth usesapiKey, whereas0.7.0 usedtoken. Existing native key transport already usesapiKey/child environment.
- **Historical pre-rotation SDK calls (not REST):** current TypeScript daemon auth failed on installed CLI0.217.0 and0.218.1. Python0.4.0 HELLO returned error_during_execution; TypeScript0.9.1/node HELLO returned401 status code (no body). Implementations were unchanged; credentials used stdin/owned environments. Preserve these observations without treating them as present blockers or proof of the user's earlier app behavior. The subsequent new-key checks above succeeded.
- **Rejected partial-attribution prototype:** caller=sdk requires metadata.sdk; supported languages aretypescript/python. The prototype's four adjusted native tests passed but the real daemon returnedRpcParseError. Reverted all24 source/test edits byte-for-byte; restored-Werror build and public-probe rebuild pass. No false branding, partial SDK metadata, codec relaxation or auth bypass retained. Four checks of exact private0.9.1 schema source confirm that the retainedhaskell-sdk/apiKey shape is accepted and partial/unsupported SDK metadata is rejected. First private-schema import attempt failed and is retained.
- **Current receipts:** ~/Products/droid-sdk-haskell/sdk-auth-20260914. native-sdk-rejected-prototype preserves the invalid prototype executable; native-sdk was rebuilt against restored source. Do not treat either a prototype fixture pass or a rebuilt binary as live success. Local reference children still observed at immediate scope return were absent on later inspection; no forced cleanup/exhaustive-census claim. File formatting and documentedfk- prefix checks pass, but are not credential-validity evidence. No endpoint/region override variables or custom-model selections were found in the inspected local configuration.
- **hsdk-fn4 resolved:** native C reproduced the intrusive macOS lseek(SEEK_CUR,0) observer replaying a header in an unchanged16MiB file (1/300; second LF at4098). Apple source shows offset read/write-back. The test now uses proc_pidfdinfo on macOS and /proc/self/fdinfo on Linux through private test/FilePosition.c; production scanner/budget/assertions unchanged. Corrected native observer0/300 over5,227,002 observations; all53 affected tests pass on both platforms, and full GHC9.12.4-Werror suites pass3,942/31.31s macOS and3,942/36.86s Linux. Original failures and initial source retained under append-snapshot-20260914.
- **Historical scalar-trace checkpoint:** original append test did not fail under tracing; four other checks failed solely on diagnostic stderr, retained as traced-integration.status1. Both trace edits were removed byte-for-byte; restored3,941/29.07s alone was not a fix. The later native replay reproduction above established the observer defect. No temporary tracing remains.
- Final tracker flush refusal was inspected at01:21:53: zero dirty issues; current PLAN SHA256 `c46f0e870b5d30bea0ed0a112b343f8881a322e9844bdf63b1e7fb28db598f6c`, plus one newline exactly equals certified export `fb2bd6724d8f646ce4ba2440b11545a9446c4a867f59ae7a77cabfad01822779`. Only the known generated EOF blank was trimmed; no unknown issue change, forced sync or merge. Whitespace remains clean. This is not a new SDK or whole-goal blocker.
- **Scoped release fess (00:45):** reviewed Cabal/Nix/CI packaging, URL checker/four tests, private-state export/comment and claims; no new actionable defect in that scope. Parent review, not a fresh audit of all accepted SDK code. The limited common-marker scan found only a verified loopback rejection-test placeholder, without printing values. `source-distribution-20260913/release-fess.md` retains the historical report; subsequent live/check/disposition results below supersede its old gate statuses. No fresh independent-review or exhaustive secret-detection claim.
- **Private documentation fix (September14,00:08):** `Internal/Session.hs` exports the already-referenced `LocalSessionState` type abstractly within its private module, with its existing comment rendered by Haddock. Constructor/representation and public facade stay hidden; no runtime logic or Generic instance changed. The target check fails before and passes after; public import still fails as required. Current -Werror all-component build, format/lint and Haddock/URL-file checks pass (98 pages/107,336 URLs); SDK-owned warning removed, other diagnostics retained. Initial verification was targeted; the current archive below subsequently supplied the full Linux suite. Parent review/execution, not an independent reviewer. `private-state-docs-20260914/` retains scope and before/after evidence.
- **Release-state reconciliation (23:51):**40 bindings agree with Obr; five original completed milestones retain their dated evidence. All152 issue rows were inspected for status, not re-inventoried for functionality. Only the nine known release/authorization rows remain required; the other two nonclosed issues are explicitly excluded. Parent/prerequisite checks on all nine found no closed parent masking unfinished required work. No status/edge changes or final acceptance. `source-distribution-20260913/release-bindings.json` and `release-dependencies.log` record results; initial JSON-envelope/original-milestone checker assumptions were corrected and retained. Do not repeat this check without a relevant state change. No fresh live approval or endpoint has arrived.
- **Historical preparation (September13,23:34):** `FollowupChecks.hs` originally had no main and was compiled only; its three-prompt local function subsequently ran successfully as recorded below. The old two-read TLS daemon proposal is superseded by the bounded owned-loopback/full-session plan in `.m3p.5` and `DaemonSmoke.hs`; neither proposal is positive daemon evidence.
- **Local follow-up passed (05:40:45):** HELLO/recall stream/result identity, first-chunk TurnCancelled, closed-query DroidSessionUnusable and observed direct-child exit passed in34.890754s with no stderr. `live-local-20260914/result.json` is real execution evidence, but did not record the PATH-resolved CLI version; its earlier inferred0.217.0 attribution is withdrawn. Current PATH later resolves0.218.1, without proving when it changed. The earlier separately version-recorded0.217.0 one-shot is unaffected. No paid rerun merely to repair this metadata gap.
- **Daemon diagnosis:** the first full attempt failed before any successful turn marker; the owned daemon exited normally with no observed remaining descendant. Auth-only Haskell probes and pinned CLI0.217.0 (including standard FACTORY_API_KEY startup environment) return-32001. Published TypeScript SDK0.7.0 gives the same authentication failure. Its isolated reference install used scripts disabled and explicit dependency pins; implementation hash matches the retained baseline. Initial missing-zod import failure is preserved. No Haskell runtime/schema defect was established, so none was patched.
- **Historical doctor diagnostic:** reported API401/not-found and telemetry DNS failures. Its key-invalidity conclusion is withdrawn as above; do not repeat it to validate an SDK-scoped key. Preserve old receipts/markers without using their obsolete conclusions as current instructions.
- **Daemon harness:** standing approval covers bounded owned-loopback verification once a changed, source-grounded setup or working SDK example provides a useful next test. Use a unique attempt label; never reset existing markers. No request for a new credential or permission merely because an SDK call failed.
- Latest refocus **2026-09-14 23:30:41 UTC**, next due **2026-09-15 00:30:41 UTC**. Scope: resolve the independent audit's documentation findings and verify the corrected distribution. No runtime/test-source, dependency-bound or compiler-flag change; no new live call. Required issue closures are restored; submit the native completion re-audit.
- **Corrected verified archive:** ~/Products/droid-sdk-haskell/audit-docs-20260914/archive/droid-sdk-0.1.0.0.tar.gz, SHA256 `f0eb3194c39d1601137ee96ccaa1ed793d75eee21e4ca46f08aabae010919c7e`. All388 files match checkout/separate unpacking. Linux all-component-Werror build and3,942 tests/37.08s pass, with Rust3/Python9, attachments,77 compile-only README blocks,192 links, notices and warning-free archive package check. Mac build/refreshed Haddock98pages/107336URLs/zero URL-file failures and package checks also pass. Runtime/test sources, README Haskell blocks and non-description Cabal fields are unchanged from the rejected9677c687 archive, retained under delivery-20260914. Separate distribution-only caches, not pristine caches or a new independent runtime review.
- **Final check/fess scope:** current Mac full suite3,942/31.31s plus supplemental all-component-Werror build, Ormolu/HLint/Rustfmt/Actionlint, nine audits, attachments, Haddock98pages/107336URLs/zero URL-file failures and package checks pass. Accepted Haddock/Generic/dependency-doc limitations and checkout no-repository advisory remain visible. New private observer only; no production workaround, weakened assertion, mock, suppression, auth bypass or new dependency download. Scoped credential/common-marker scan found no current key in the388 packaged files; sole unchanged marker is a rejected placeholder URL in RESTSpec.hs:164. No exhaustive security or new independent-review claim.
- Earlier documentation configuration fixes: `cabal.project` supplies versioned Hackage URLs and package-scoped `haddock-options: --show-all`; those configuration changes did not alter Haskell exports. The fresh before/after test removed55 missing-file failures, with98 pages/107,330 URLs checked at that boundary. Four checker regressions plus five audits passed (9 total); build, Actionlint and package check passed with the no-repository advisory retained. Receipts/fess: `haddock-modules-before*`, `haddock-modules-after*`, `module-link-*`, and `docs/development.md#dependency-documentation-url-correction`.
- **Documentation disposition accepted:** under the user's standing delegation for noncritical decisions, the reproduced upstream instance/Generic links and missing attoparsec documentation are explicitly nonblocking limitations. They remain published in `docs/development.md#september-14-release-verification-and-documentation-disposition`, not claimed fixed. SDK-owned destinations are fixed; generation, URL/file checks and warning-free SDK builds remain mandatory. No suppressions, runtime-instance rewrite, toolchain fork or fabricated repository URL. This closes `.jvc`, not live conformance or overall release certification.
- Platform acceptance **20:24:54**: fresh six-marker attachment FD/race checks pass on both GHC9.12.4 ARM64 hosts, supplementing the3,941-test suites. macOS26.6.2 and the existing no-network Linux route were used; native/runtime and Input/driver source equality was checked. Scope/fess: `docs/development.md#ghc9124-platform-verification`; `macos-attachment.log`, `linux-attachment.log`, statuses and `platform-*.txt`. No x86_64/musl or hosted-CI execution claim.
- Bounded documentation inspection **07:10–07:25** read existing receipts/registrations only. Current Cabal units are `ttprsc-0.14.4-1018e56b` (internal) and `ttprsc-0.14.4-556b129a` (public); both registered Haddock interface files are absent. Earlier documentation experiments are not current-store interface installation evidence. Initial global-registry and unabbreviated-name lookups were wrong; the actual plan/exact registrations resolved them. No package DB, cache, SDK or compiler was changed, and no missing-library/build-failure claim follows. The stock-Generic warning remains supported by the retained minimal probe, not a fresh run.
- **Approved local attempt consumed and passed:** installed CLI0.217.0/GHC9.12.4, one HELLO prompt in empty Products cwd, existing login/model, denied permissions/cancelled questions,30s turn/45s outer cancellation deadline plus cleanup, no retry. First nonempty chunk10.408937s; matched result/session; SDK scope12.850164s; monitored total13.723947s. Both observed direct child PIDs exited and stderr was empty. `stable-ghc912-20260913/live-result.json`, `LocalSmoke.hs` and the exclusive attempt marker preserve scope/evidence. No daemon, Git/account/trust/resource mutation or publication.
- Stable-compiler checkpoint evidence: -Werror builds and3,941 tests pass on macOS ARM64/29.87s and GNU/Linux ARM64/61.58s; all77 README blocks compile on9.12.4. Full format/HLint, Actionlint and the original five audits/integrity pass. The Linux384-member snapshot predates the later documentation/configuration/scripts changes; runtime/Input/driver continuity was verified for the platform checks, not current archive equality. Subsequent URL-checker tests raise Python checks to8. No new sdist or remote CI run is claimed.
- Obr rejected claiming aggregate local-live `.m3p.6` while broader authorization `.m3p.5` remains open. Its partial success evidence was appended without changing status/dependencies or repeating the live run. Compiler `.m3p.1` was genuinely closed after dependency checks and its receipts; broader live tasks remain incomplete.
- Contract acceptance **06:48:12**: `hsdk-9j2`, `hsdk-w8y`, `hsdk-haskell-attribution-80m`, protocol-baseline parent, attribution/observability parent and required-operation coverage are closed/verified. Required child trees were queried before parent closure; dashboard **33/45**. Native tests retain valid timestamp spelling, reject thirteen coercive/invalid values in each of two fields without replacing seeded state, preserve connection usability, and verify absent default metadata/truthful daemon caller. Leap occurrence forecasting is explicitly nonblocking scope; MCP's separate second60 rejection and existing profile codec are unchanged.
- Current policy receipts: `~/Products/droid-sdk-haskell/contract-policy-20260913/`. Baseline306/0.38s; expanded focus310/0.42s; all3,941/31.53s with -Werror build, changed-file Ormolu/HLint, five audits/integrity and181 local package links. Initial Ormolu findings were fixed and retained. Fess is parent source review/execution, not fresh independent/live/platform/archive proof. The only production-source edit is metadata Haddock; runtime policy was already present. Full evidence and limitations: `docs/development.md#approved-timestamp-and-attribution-contracts`.
- Reload ACK correction accepted **04:36:07**: `performSessionReload` now uses the existing `awaitDaemonCommand` around its admitted restriction write. It retains session/request correlation, generation/auth admission and the existing30s patch budget; pending handlers/readiness wait through ACK. Three native regressions cover delayed/wrong-correlated completion, malformed completion and async cancellation. Full **3,938 /31.18s**, -Werror build, changed-file format/lint, five audits and integrity pass. `.wvf.1` and offline conformance `.m3p.4` are closed/verified; dashboard **27/45**. Parent protocol/matrix/live gates remain open. Evidence: `~/Products/droid-sdk-haskell/reload-restrictions-20260913/`; Linux attachment FD/race script additionally passes six markers under `offline-conformance-20260913/`.
- Bounded reviewer `a6f0840e-9930-405c-88b7-10d1f7b76536` found the sole P1 above and no other gap in the inspected non-temporal wire groups. Report: `wg-required-wire-20260913/review.md`. The initial workflow `d2c8727d-9740-40a0-9068-1a301ca463e7` rejected probe bookkeeping for missing no-staged-files evidence. Parent canonical history verification and empty-index check passed **04:00:25**, then direct revival continued the same fresh probe session. Reviewer ran no tests/git; reproduction and fix verification are parent execution. No further review cycle was run.
- Diagnostics (`~/Products/droid-sdk-haskell/docs-diagnostics-20260913/`): `de --max-jobs 4` completed the declared vector-documentation extension. The actual `vector.haddock` exists; GHC9.10.3 and vector package ID `vector-0.13.2.0-3t4MFx9Vg86InKQPSIBbvW` are unchanged. Ordinary SDK -Werror build and **3,935 tests /32.42s** pass; ordinary Haddock no longer reports vector missing. `after-vector.log`/`.status`, `vector-interface-verified.log`, `de-vector-docs.log` preserve execution. Raw Haddock2.31.1 still emits `Rep_Probe` for a ten-line stock-Generic module; private attoparsec documentation and confirmed source-repository URL remain unresolved. No instance removal, bounds relaxation or warning suppression.
- Documentation/example acceptance **03:28:30**: `hsdk-docs-package-signoff-ip3.4` closed/parity-verified; dashboard **26/45**. All77 README Haskell blocks compile with -Werror:59 verbatim,11 declarations with module/import context,7 expressions in typed IO entry points. No example was executed. Migration/setup/context guidance and stale current-status prose were corrected; seven checkout-only links and three bad anchors were fixed. All180 local Markdown links pass in checkout and unpacked archive; five reference audits/integrity pass. Parent self-review/fess only. Receipts: `~/Products/droid-sdk-haskell/documentation-examples-20260913/`.
- Previous documentation archive SHA256 `77cfd738131e020a6475933b33fb81c4249dc811230a8fb82b498ec50532460e` matched384 files and180 local links at its checkpoint. It predates the reload fix and current policy documentation/tests; do not call it current-runtime evidence. The older ba7e48… archive has an independent Linux build/test receipt. No new archive cycle was used to hold up accepted capability work; `.6` and final gate `.5` retain their release requirements. No active build or child remains.
- Cabal `haddock-project` was investigated, not adopted: ordinary/library-only/internal variants build dependency docs but retain missing private attoparsec and generated-Generic destinations, and add missing-Haddock warnings for C-only/test components. Library documentation is produced; no warning-free claim. Its builddir argument is not propagated to all internal steps, so probes stayed in copied Products source trees. Mac's offline-mode refusal affected70 already-cached archives; rerunning with the curl transport replaced by a failing guard used the existing files successfully. No ad-hoc dependency or live Factory call occurred. The source-repository URL remains unset pending confirmation; no URL was invented.
- Linux route: `de --max-jobs 4 path:.#linuxChecks` realizes the pinned public Linux GHC/Cabal/GCC/Cargo/Rust tool closure. Only its **167 paths /5.1 GiB** were mirrored under Products and checked for identical NAR hashes, avoiding a mount of the full host store. The existing image `sha256:b515b04bfd25298a5cdc775b2fcd48b9399bab658ce13e2598b65df1b16098c8` is used with `--pull=never`, ARM64, no network, read-only root/store, dropped capabilities, no-new-privileges and the unprivileged caller UID. Writable state is confined to the copied source/build workspace and container tmpfs.
- Linux native proof **22:41:24**: GHC9.10.3 on Linux7.0.12-linuxkit/aarch64 passes C IPC stdio/stderr/IPC/CLOEXEC/same-PID ownership/reaping/exec-error and birth-time statx/mtime-independence/EBADF checks, with birth available. Linux Rust builds and passes three tests. The corrected full SDK passes **3,935 tests /37.62s**, five audits/integrity and package check (`linux-full-final.log`); a fresh source archive also builds/tests its own worker and SDK successfully (`linux-sdist.log`/`.status`). The archive predates only the diagnostic wording correction. All isolated source inputs are public and hash-verified; no network is available at execution.
- Nine initial Linux failures came from test assumptions. Validator executable fixtures now respect `TMPDIR` instead of hard-coding the `noexec` `/tmp`; only the IPC self-peer explicitly re-decodes UTF-8 argv under empty environment. A fresh Haskell program proves inheritable control pipes predate process use; the privacy fixture marks that pre-IPC baseline CLOEXEC once. All inner exec/check behavior remains. Removing Linux `MSG_CMSG_CLOEXEC` still fails on newly allocated pipes/socket; copied C source was restored, compared with canonical C, rebuilt and passes **17 targeted checks /2.36s** (`linux-descriptor-mutation.log`, `linux-restored-focus.log`).
- WebSocket diagnostics prove worker cancellation/identity checking finished before the tiny receive-window drain timed out. A late window increase failed and was reverted. Current test uses64KiB receive/16MiB payload/32MiB limit, explicitly waits for the sender to be blocked, and retains AsyncCancelled, peer EOF-or-reset and the original10s bound. The Linux review found the old “EOF” label overprecise; it now says closure, without narrowing reset acceptance. Mac9.12.4 full regression passes **3,935 /32.10s** and full format/lint/five audits/integrity pass (`macos-regression.log`, `post-linux-tooling.log`).
- Independent Linux fixture review `e7c54a88-7f81-413c-837c-27a2c34e1877` completed in the verified fresh delegate session. Report: `~/Products/droid-sdk-haskell/wg-linux-fixtures-20260912/report.md`. No blocking regression or waived assertion; parent fixed the low diagnostic wording issue. Reviewer executed no tests. Linux9.12.4 subsequently builds warning-free and passes **3,935 /37.61s**, package check clean (`linux912-sdk.log`/`.status`). Mac9.10.3 again passes **3,935 /33.33s**, formatting and separate completed HLint. Default direnv is restored to **GHC9.10.3**; `run-with-tools` selects stored Linux closures explicitly.
- Acceptance **00:51:50**: launch/owned IPC, saved timestamps, native-host5/5, advanced-transports2/2, discovery3/3 and discovery/configuration3/3 are closed/parity-verified after actual descendant queries. Original descriptions, acceptance, bindings and note history were preserved. Dashboard **25/45**. Receipt: `linux-checks-20260912/capability-acceptance.json`. Linux Haskell FileTime matches independent `unix` statx exactly and rejects invalid FDs; four scanner/birth tests pass. Its rejected wall-clock bracket and corrected exact oracle are retained in the fess.
- Previous runtime archive verified **01:44:28**: SHA256 `ba7e48b77ed9f7e5c6887bf8fe4dd10e277bd7f93a4499ed31d246daf8a8b384`; all384 members matched canonical source at that boundary. New Linux/GHC9.10.3 build directories produced its own worker/launcher/private peer/SDK/tests/example; Rust3 and Haskell**3,935 /37.62s**, five audits, integrity and package check passed. Worker selection was asserted and its `--licenses` bytes matched shipped notices. Receipts: `current-sdist-build.log`/`.status`, `current-sdist-equality.json`, `current-license-equality.log` under Products/linux-checks-20260912. Later documentation/environment differences are identified above; no final release acceptance.
- Compiler work: named Nix shells select exact GHC9.12.4 and9.14.1 beside the unchanged9.10.3 default. The first9.14 shell unnecessarily preloaded development libraries and their test dependencies; its owned Nix client was interrupted, and matrix shells now use bare compilers so Cabal checks the ordinary dependency graph. Both compiler binaries report their exact expected versions through direnv.
- Strict GHC9.14.1 resolution fails on `tls -> serialise-0.2.6.1 -> base <4.22`; all permitted TLS versions retain that dependency. Hackage revision5 still has this bound; upstream issue `well-typed/cborg#373` reports builds with9.14 but is not a published metadata fix or local execution evidence. No `allow-newer`, bound waiver, crypto downgrade or matrix removal was applied. Preserve `ghc914-plan.log` and `ghc914-tls23-plan.log`.
- Bare GHC9.12.4 strictly resolves and builds all SDK components with `-Werror`; **3,935 tests /32.21s** pass. Its independently unpacked source also builds and passes **3,935 /32.07s**, Haddock completes, package check succeeds in the archive, and reference integrity passes. `tested-with` records9.10.3 and9.12.4 only. Receipts: `ghc912-verified.log`, `ghc912-package.log` and status under `~/Products/droid-sdk-haskell/compiler-matrix-20260912/`.
- The original9.12 suite reproduced the validator's cold-launcher timing assumption. Native close-FD startup measured101–103ms at an FD limit of1,048,575; fresh fixture worker starts measured314–1198ms versus104–109ms on immediate reuse. Only the two engine-deadline fixtures now preflight the real worker, verify reaping and remove the marker before timing. Original500ms budget, two-second bound, fresh-PID and reaping assertions remain. No production timeout or FD-security behavior changed. Cold-start completion within500ms is not an API promise. Details/fess: `docs/development.md#compiler-matrix-checkpoint`.
- Thirty-six missing solver-selected archives were provisioned via temporary Nix fixed-output inputs, hash-verified and atomically made available in the existing Cabal cache. The temporary manifest/flake attachment was removed from the repository after use; snapshots remain under `source-provisioning/` in Products. Only named bare compiler shells and truthful compiler metadata remain. The first cache script misread the single-field `cabal path` format, then was corrected before copying. No archive overwritten, source-bound waiver or ad-hoc download.
- Current post-audit IPC checkpoint: warning-free all-component build, **3,935 full tests /29.79s**, full Ormolu/HLint, five reference audits/integrity and the exact compiled README example pass. **16 IPC cases** now include writable duplex stderr, ordinary/IPC blocking-mode comparison with complete 256 KiB drain, and actual blocked socket read/write cancellation before bounded cleanup/reaping. Focused process/IPC/validator boundary: **50 /7.89s**. Receipts with exact commands: `post-audit-integration.log`, `post-audit-tooling.log`, `audit-stderr-before.log`, `audit-stderr-after.log`, `after-audit-focused.log`.
- The privacy check isolates unrelated pre-existing IPv4 connections in a fresh Haskell process; its 100 inner unrelated execs still use `close_fds=False` during 20 owned launches. Deliberately removing IPC socket CLOEXEC makes that test fail on an AF_UNIX descriptor. Original C source was restored byte-for-byte and rebuilt; no assertion or compiler policy was weakened.
- Actual **Node v22.23.2** stdio/IPC/stderr exchange, small-domain exact values and PID reaping were rerun against the post-audit library. The replacement sdist independently builds/tests its own Rust worker, C launcher/private peer and Haskell package, then passes **3,935 tests /29.38s**, five audits and integrity. **370** source/test/native/schema members, Cabal metadata, README and parity/development docs match the checked archive. SHA256 **`05a92a8ae9f161ffb653018dd098b970554b1d16f20d40f55173af91aefeca04`**; receipts `post-audit-sdist-build.log`, `post-audit-sdist.status`, `post-audit-archive-equality.json`. This is not Linux or final SDK-release acceptance.
- Independent read-only audit `7061b0d6-42e8-48df-99db-d46cb3563208` completed. Canonical history isolation passed **18:37:33**; initial workflow's unrelated no-staged-files bookkeeping rejection was resolved by reviving the same fresh child with verified empty-index evidence. Report: `~/Products/droid-sdk-haskell/wg-owned-ipc-20260912/audit/report.md`. It found two medium stderr defects; parent reproduced both (`InvalidIpcProcessOptions` for writable duplex, and `nonblocking=True` versus ordinary `False`) and fixed them using GHC writable-side selection plus all-halves blocking-mode/device normalization. The audit did not execute tests or inspect subsequent fixes; those are parent-verified. Full batch fess: `docs/development.md#owned-child-ipc-implementation-and-fess-checkpoint`.
- **Environment corrected:** `de --max-jobs 4 --option builders '' path:$PWD` completed successfully by **16:58:43**. Commands run through direnv: GHC **9.10.3**, Cabal **3.16.1.0**, Rust **1.97.1**, native validator and format/lint tools. Earlier `--max-jobs 0`/extra-approval blocker was an agent mistake. Cache relocation forced a rebuild; a 150s tool timeout was resumed with no overlapping project writer. Initial baseline passed 33/34, with one validator deadline expiring before its PID marker; unchanged 10-worker recheck and subsequent 46-case process/IPC/validator boundary passed. Cause of that first timing failure is not established. Receipts: `~/Products/droid-sdk-haskell/owned-ipc-20260912/`.
- Cabal resolution selected a different already-installed zlib unit through streaming-commons, requiring warp/websockets rebuilds. Their cached archive SHA256 values matched the plan. Cabal's `--offline` nevertheless refused them; they were rebuilt from those archives with the selected curl transport disabled, without downloading packages or changing versions. Normal `--offline` checks now work. Preserve `cabal-*-diagnostic.log`, `native-ipc-cached-build.log` and the first failure; do not report the failure as missing archives.
- Existing generated `dist-newstyle` was moved to `~/Products/droid-sdk-haskell/dist-newstyle`; the original path remains a symlink, preserving cached paths. Put new logs/probes under `~/Products/droid-sdk-haskell/`. `de` currently computes CABAL_BUILDDIR in this worktree for its `~/work` layout; explicitly pass the Products build directory to Cabal rather than creating new build outputs in the tree.
- Two real regressions were reproduced before fixes (`stderr-regressions-before.log`): borrowed stdout lost child stderr, and a full borrowed-stderr pipe escaped the startup deadline. Native file actions now map stderr before replacing fd 0/1. One timeout covers preparation/handoff while a masked ownership cell keeps native/ProcessHandle cleanup alive across timeout's return; the existing cleanup owner and `finallyPreserving` are reused. The same regressions and all **46 process/IPC/validator cases pass /8.09s** (`ipc-lifetime-regressions.log`). No assertion or warning suppression. OS spawn/filesystem calls and final reaping remain non-preemptible constraints, explicitly documented.
- Owned-IPC proof (`/tmp/droid-owned-ipc.TFMmVxCe/`): native fileport check passed04:34:22, within thirty minutes of04:22:17 start. Published XNU code sets FP_CLOEXEC under the FD-table lock before publication (retained source SHA256 `480cfed4e987be874bd71fb6933c254adf9fb1f36de8496dee8f351b18da13b1`). Same-PID exec, private Mach transfer, bidirectional IPC, unchanged/restored bootstrap ports, reaping and100 unrelated execs pass04:43:16. C-only Cabal packaging passes04:53:27. The SDK C bridge passes stdio/stderr/IPC, CLOEXEC, adoption/reaping and exec-failure checks05:16:17. Earlier Nix-header, HandleType, conditional-compilation and deprecated-Mach-API failures are retained; the library/launcher subsequently built05:35:42. Current Haskell/native integration remains unfinished as recorded above; no SDK acceptance or Linux execution.
- Timing gap: the preceding recorded check was **00:39:33**, due **01:39:33**. The first clock after the checkpoint was **03:33:23**, **1h53m50s past that deadline**; no timely intervening check is recorded. No uninterrupted hourly-compliance claim.
- **Hosted acceptance 04:14:40 UTC:** `hsdk-mcp-y1i.1` and parent `hsdk-mcp-y1i` are closed/verified; all six required descendants were queried. Dashboard **21/45**. All raw/typed/structured styles execute through actual local/daemon peer startup/load and local replacement/rollback; HTTP worker loss and close/reaping are also checked. **34 focused checks /20.46s**, **3,919 full tests /28.22s**, build **8.19s**, full tooling/five audits/exact example pass. Only tests/helpers/docs changed after validator acceptance. Full fess: `docs/development.md#hosted-session-integration-acceptance`; receipts `/tmp/droid-hosted-integration.FmEXHsUc/`. First changed check began and passed within thirty minutes of the03:46:27 claim. A formatting failure and a combined-tooling timeout are retained; the remaining checks passed separately.
- **Validator acceptance 03:44:49 UTC:** `hsdk-zpf.1/.2/.3`, their parent `hsdk-zpf` and licensing `hsdk-4po` are closed/verified. Prerequisites were honored, descendants queried, and original descriptions/acceptance/bindings/notes preserved. Dashboard then **20/45**. Full batch fess: `docs/development.md#native-hosted-validator-integration`.
- Validator verification: the worker-local regex repair fixes the reproduced false result and its inversion under `not`; remaining resource failures exit explicitly. **31 focused tests /8.54s**, **3,917 full tests /20.85s**, warning-free build/full tooling/five audits and exact compiled README example pass. Real compilation and regex deadlines require executed workers and verify reaping; cancellation retains `AsyncCancelled`. The worker uses existing process ownership, empty environment, closed unrelated descriptors/stderr and bounded exact-value JSON.
- Native deployment and distribution: locked offline Cargo installation works; notices for **65** dependency/version pairs across four target graphs are bundled and match `--licenses`. The former Haskell dependency is absent from the Cabal plan. An unpacked sdist independently builds the worker (**16.68s**) and Haskell package, then passes **3,917 tests /21.80s** using that worker. The first Haskell build hit a 240s tool timeout near completion; the resumed build finished in18.01s. Only macOS ARM64/GHC9.10.3/Rust1.98.1 execution is established. Archive SHA256 `a8d2568d3ad44ec5bda5ff2f6be630be55a7ad7ae57f3468b2269ae31b83f326`; receipts `/tmp/droid-validator-worker.37VmS8CX/`. No live SDK, image pull, commit or publication.
- Selection acceptance: began **21:17:56**, source reconciliation finished **21:29:31**, first changed check **21:31:56–21:32:49** passed **28 prior cases / 0.34s**. The first 23 selection cases reproduced root-file `/.favorites` ENOTDIR; a root directory check fixed it, and the same cases passed **0.15s** by **21:47:43**, within thirty minutes. A reviewer-requested zero-I/O regression raised the count to **24**, all passing with two Tasty workers / **0.10s**. Final **3,882 tests / 12.74s**, Cabal **16.13s**, build **13.11s**, full 162-file formatting/lint, five audits/integrity and three exact compiled examples pass. Receipts `/tmp/droid-saved-session-selection.C68hZhyW/`; full fess `docs/development.md#saved-session-selection-delivery`.
- `listDroidSessions`/options/selected view are public in Discovery and the facade, reusing raw file observations and descriptor owners. Canonical/lexical directory conventions, root favorites, filter-before-dedup and final deterministic limits are preserved. Ten original TS, ten original Python public-list groups and the linked native API pass the same ten fixtures under explicit combined semantics, including the TS oversampling defect. Default/relative/empty/tilde native self-peers isolate HOME/cwd. Reviewer `4f6daac0-330b-43dc-ae5d-de3f02358f3e` found the zero-I/O test gap, now corrected; workflow `41fd82cd-e5ef-4130-9303-fc9525d9768a` completed. No sentinel/reviewer execution, Linux or independent-sdist certification.
- Timestamp receipts: `/tmp/droid-saved-session-times.x9Ad0j4x/`. Began **20:24:40**; fresh unsupported unix probe **20:30:45–20:30:47** reported `haveStatx=False`. Native C Darwin results matched Node bigint nanoseconds and EBADF at **20:37:09**; linked Haskell nanoseconds matched at **20:53:05**, and actual Haskell errno propagation passed **21:03:47**. One header-driven bridge uses Darwin fstat or Linux descriptor statx with returned birth-mask checking. The existing owner captures optional birth metadata before streaming, without unused settings/favorites queries. `sessionFileBirthTime` preserves provenance; `sessionFileCreatedAt` explicitly supplies the reference-compatible ctime fallback only on absence.
- Timestamp checkpoint verification: **four new cases; 28 scanner tests / 0.35s** and full **3,858 tests / 12.88s**, Cabal **17.34s**, all-component build **15.15s**, full-tree formatting/lint and five audits/integrity pass. Explicit Ormolu target list contains **161 files**. Native C11 warning/pedantic check, exact compiled timestamp example, real nanosecond/EBADF probes and Cabal source membership pass; no independent sdist claim. Reviewer `b79ab476-6cda-4e7b-bc45-a5dd6ff4acf1` found no static issue; workflow `d832a44c-e560-482c-abef-93813566f5c8` completed. No sentinel or reviewer execution; pure fixture projections are not C-conversion tests. Fess `docs/development.md#saved-session-timestamp-checkpoint` preserves CLI/format/ad-hoc link failures and evidence limits.
- Linux availability was checked once: a local Unix-socket Docker daemon runs Linux/aarch64 (`7.0.12-linuxkit`), but sole cached ConvertX image `b515b04bfd25` has no cc/gcc/clang; no host Zig was found. The ephemeral availability container was read-only, network-disabled and capability-dropped, with no source mount or app launch. No compiler image was pulled, no Linux bridge ran, and no Linux Haskell SDK result is claimed. Parent `hsdk-f20` now has **2/3** closed children; `.3` remains partial/in progress. Dashboard **17/45**.
- Saved-session scan `.1` **closed/verified offline 20:14:19**. Began **19:22:52**; first `-Werror` build **19:37:14** met thirty minutes and retained a name-shadowing failure. Source reconciliation ended **19:33:42**, but lookup-budget accounting exceeded the planned allowance despite the recorded extension; compliance is not claimed. New `Factory.Droid.Discovery` provides default root, direct-directory/read-one metadata and favorites, with regular-file identity, nonblocking/CLOEXEC, observed-size bounds and cleanup. **24 new cases; 58 scanner/attachment tests / 0.17s**, build **10.48s**, Cabal **3.72s**, five repeated scanner runs, later UTF-8 checks **24 / 0.12s**, changed-file tooling and an exact compiled example pass. After removing global HOME mutation from a test, **24 cases passed with two Tasty workers / 0.14s (20:20:31)**. Seven TS and five Python source groups execute original code/schemas/dataclass, without SDK initialization. Receipts `/tmp/droid-saved-session-scan.nwXvhGDX/`; full fess `docs/development.md#saved-session-file-scanning-delivery`. No new full-suite result beyond the preceding 3,830 boundary.
- Discovery boundaries: stable symlinks are followed, not sandboxed; raw header/settings/cwd and typed projections are retained. Title normalization/optional-field/UTF-8/line-count/archive differences are explicit in parity docs. Timestamp provenance is separate from the compatibility projection. The historical `/tmp/droid-discovery.T3DGAT` is absent and was not re-presented as fresh evidence. Initial scanner reviewer `d2467d20-c228-48fe-97c1-68462e8b3321` completed static review, but wrapper `87913b0f-e24a-40d5-bd1a-135aabc53b50` lost continuation and failed. No sentinel or reviewer execution. Later selection acceptance is recorded above; Linux timestamp verification remains open.
- Stream/helper parent `.2.2` is **closed/verified offline (19:14:46)**. All **7/7** mandatory children were queried and verified at **19:12:43**. Shared boundary: **3,830 tests / 12.60s**, Cabal **16.45s**, all-component `-Werror` build **8.42s**, full-tree formatting/lint, five audit tests and integrity. **82 new helper cases** reconcile with the 3,748 baseline. Ten exact README examples compile/format/lint; eight cached probes pass **51 selected runtime/control groups + four structural checks**. Parent self-review only; no fresh independence, live, other-platform or distribution certification. Full fess `docs/development.md#streamhelper-parent-acceptance`; proof `/tmp/droid-utility-helpers.0yfgL9Aa/parent-integration.json`. Dashboard **17/45**.
- Utility `.2.2.7` is **closed/verified (19:10:41)**. Began **17:35:07**; first check **17:43:34–17:44:47** passed **235 usage/mission tests / 0.03s**, within thirty minutes. Shared usage/summary/pending logic and all mapped pure projections are public in their owners. **23 new utility cases** plus the separately counted raw-tag correction are in the final suite. Nine extracted source groups include actual cached Zod; equality remains a structural source check, with native execution and no substitute comparator. Native nested metadata retention/existing 1.205.0 validation differ explicitly from the older normalized copy. Unused-import and formatting failures were fixed with receipts retained. Secondary static reviewer `f75df8a3-246a-43c1-9661-677cdf286e7a` found the unused test import; no sentinel or reviewer execution, and later changes were self-reviewed. Full fess/test-by-test evidence `docs/development.md#utility-helper-delivery`.
- Tag `.2.2.5` was **reopened 18:14:06 and reclosed 19:10:41** after the original probe exposed a missed numeric calling-metadata contract. Typed selection alone was insufficient; `inspectSubagentSessionTag` now returns the first raw object without relaxing the string-metadata codec or admission. Two new regression cases, the original four tag cases, a raw-source check and the exact raw README example pass in parent integration. Original closure history and its overclaim remain documented under `docs/development.md#raw-subagent-metadata-correction`.
- Queue-review `.2.2.6` is **closed/verified offline**. Began **17:19:05**; first check **17:23:14** reproduced missing API GHC-76037, then the same example compiled. Pure display group/optional priority derive reviewability without losing zero; all five kinds and prior storage/placement operations remain. **Two new cases; 22 focused tests / 0.02s**, Cabal **2.00s**, all-component build **8.18s**, changed-file formatting/lint, six source groups and exact compiled example pass. Example indentation failure retained/fixed. Self-review only; full fess `docs/development.md#queue-review-helper-delivery`, receipts `/tmp/droid-queue-review.YyGX0br8/`. Helper **6/7**, dashboard **16/45**.
- Historical subagent-tag `.2.2.5` acceptance (**corrected above**): began **16:59:40**; first check **17:06:15** reproduced missing export GHC-61689. `findSubagentSessionTag` covers typed first-tag presence and string IDs; the daemon reuses the presence test, and state admission is unchanged. **Four new cases; 78 focused / 0.51s; 3,803 full tests / 12.17s**, Cabal **14.44s**, build **2.90s**, tooling/audits/integrity and typed example passed. That evidence did not cover arbitrary JSON metadata: the earlier complete-helper claim was overstated. Original self-review/fess and receipts remain `docs/development.md#subagent-tag-helper-delivery`, `/tmp/droid-subagent-helpers.Jtaq2wu2/`.
- Message-chain `.2.2.4` is **closed/verified offline**. Began **16:42:20**; first check **16:47:07** reproduced the missing public export (GHC-61689), then the same example compiled after exporting existing `persistedHook`. Runtime bodies are exactly unchanged. **Four new cases; 92 focused tests / 0.17s**, Cabal **3.65s**, all-component `-Werror` build **7.65s**, changed-file formatting/lint, eight extracted source groups and exact compiled README example pass. Self-review only; full fess `docs/development.md#message-chain-helper-delivery`, receipts `/tmp/droid-message-helpers.caHkhSre/`. Helper **4/7**, dashboard **16/45**. No new full-suite or release certification.
- Prior refocus **15:57:07** was missed across the checkpoint gap; the first subsequent clock, **16:21:15**, was **24m08s late**. The mistaken historical-pause announcement was corrected from the running goal; full functional parity and authorized offline continuation remain unchanged.
- Legacy idle completion `.2.2.3` is **closed/verified offline** (**16:37:24**). Began **14:52:57**, claimed **14:57:07**; first changed check **15:06:52** passed **223 existing tests / 5.82s** by **15:07:50**. The shared typed feed now supplies separately selected non-idle→Idle observation with absent/latest usage, exact low-level event ordering and caller-owned subscription cleanup; normal turns remain explicitly correlated. **16 new cases; 3,795 tests / 12.59s**, Cabal **15.62s**, warning-free build **9.44s**, full tooling/five audits/integrity, six extracted Python iterator-control groups plus one structural converter check, and three exact compiled README examples pass. The source fixture boundary does not certify Python codecs. Static reviewer `537330f7-4577-4aa0-9779-9c4f7ad1a996` found no issue; final mixed-family coverage/comments/docs were self-reviewed/tested. No sentinel or reviewer execution. Formatting and redundant-void lint failures remain recorded. Full fess `docs/development.md#legacy-stream-delivery`; receipts `/tmp/droid-legacy-stream.zO78iTiE/`. Helper **3/7**, dashboard **16/45**; no live/other-platform/distribution/commit certification.
- Standalone stream `.2.2.2` is **closed/verified offline**. Started **13:36:49**; first execution **13:46:12** passed 192 local cases and changed check **13:52:10** passed 204 local/injected cases. `Factory.Droid.Stream` exposes scoped feeds and the collector used by normal turns, pure accumulation/manual optional-usage summaries and shared output adaptation. **31 new cases; 3,779 tests / 11.21s**, Cabal **13.04s**, warning-free build **4.12s**, full formatting/lint/five audits/integrity, eight extracted TS checks and two compiled README examples pass. Missing timeout cause after consumer close was reproduced/fixed; race-fixture GHC-83865 and lint/format receipts remain. Reviewer `37c4a440-8d7a-45e6-b051-de7437b20815` completed static review; wrapper `1e7af480-947c-4987-86ae-9412f37b25c1` lost continuation. No sentinel/reviewer execution; later timeout/pure changes self-reviewed/native-tested. Fess `docs/development.md#standalone-stream-delivery`; receipts `/tmp/droid-standalone-stream.eAMbKne6/`. Helper parent **2/7**, dashboard **16/45**; no live/other-platform/current-distribution/commit certification.
- Historical helper-mapping acceptance: `.2.2.1` is **closed/verified**; the parent then had **1/7 children complete**. All six existing implementation children were refined with original descriptions/notes, acceptance, bindings and dependencies preserved. Mapping began **12:48:58**; its fifteen-minute investigation ceiling was missed after prolonged analysis and another unnecessary historical-pause detour. Post-compaction refocus was delayed. First execution **13:13:46** passed **214 existing local/child tests (5.84s)**; corrected group selection then passed **63 message/queue tests (0.03s)**, **277 existing tests total**. Ten extracted-runtime helper groups and two explicitly structural source checks pass. Three historical API probes compile; two exact current README examples separately compile/match. Retained failures: source-extraction syntax error and wrongly presumed README example identity. Reviewer `f4761443-eccf-4169-b176-0d1966d5d237` found no bounded mapping issue; later comparison/enum documentation corrections were self-reviewed. No sentinel or reviewer execution. Full fess `docs/development.md#stream-helper-mapping-delivery`; receipts `/tmp/droid-helper-map.Q1UPfvkd/`. No production/test changes or new full-suite/distribution claim in mapping.
- Configuration/refinement acceptance remains **closed/verified offline**: four verified children, **3,748 tests / 11.53s**, warning-free build/full tooling/five audits/integrity, three compiled examples and 31 selected source checks. Receipts `/tmp/droid-refinements.r04pkpBX/`; fess `docs/development.md#operation-refinement-reconciliation` and `#configuration-parent-acceptance`. No reviewer execution/sentinel attestation. Configuration **4/4**, relay **5/5**, native-host **4/5**. Do not repeat the closure audit, invent schema quotas or bypass attribution/platform/live/validator/IPC gates.
- Goal unchanged: complete native functional SDK parity. The user confirmed resumption again at 03:58 UTC; the live goal remains running. `session-state` and `daemon-resources` are now complete offline. Do not reinterpret stale tracker-only prose as a new pause.
- Scope correction: the completed tracker-only amendment no longer pauses SDK work. No repeated inventory, forecast gate or issue-tree redesign. Existing live/outreach/publication restrictions remain. At this continuation I mistakenly announced another pause from the historical amendment; the live goal is running and authorized offline work continues without a new resume gate.
- Lifecycle/request-hook leaf `.1.2.5` and relay parent `.1.2` are **closed/verified offline**. Hooks started **05:52:51 UTC**; investigation/design exceeded its ceiling, while first meaningful check **06:18:04** passed 34 existing RPC cases. Shared Protocol/dispatcher hooks now cover optional caller-thread guarding, mandatory in-flight authentication waiting, settlement/pending count, close/error and identity/kind. Local startup and exact daemon attachment subscriptions use the same path. Reviewer `1c5e054c-16f9-4a44-bbe1-627b168a2eaa` found queued auth-error loss; native reproduction failed expected `Just CallbackFailed`/got `Nothing`. Existing auth lock now retains the outcome. An over-broad new auth restriction was corrected without weakening prior post-logout authority checks. Final **3,698 tests / 12.17s**, **8.15s** build, **15.65s** Cabal test, full tooling/five audits/reference integrity pass. Nineteen hook cases plus strengthened adapter tests. Static review only, no sentinel or reviewer execution/fix follow-up. Parent acceptance recompiled all five exact README examples and reran five cached probes (47 selected checks); 97 relay cases are in the full boundary. Fess: `docs/development.md#connection-hook-delivery` and `#relay-orchestration-parent-acceptance`; receipts `/tmp/droid-connection-hooks.MPFsdVuN/`. Relay **5/5 complete**, native-host **4/5**, dashboard **14/45**. No live calls, new compiler/platform/distribution certification, commits or publication. Next ready leaf: `.2.1.1` configuration mapping.
- Completed connection-readiness leaf: `.1.2.4`, started **2026-09-11 03:18:08 UTC**, closed/verified offline. Initial mapping exceeded fifteen minutes; first build/check **03:40:44** met thirty minutes but selected only eleven load tests, followed by 23 pending/health tests. `Connection` now holds existing daemon/relay scopes on one worker, coalesces polling, implements pre-spawn/auth budget rules, ensure-running and remembered status. `Daemon` repairs authentication in place with principal/epoch/current-token protection; session readiness is reused. Reviewer `09df4a3a-9bc0-4128-83d8-7f2d110cb8cc` identified P1 old-teardown/queued-poll ownership; native reproduction failed True/False. One pending work slot plus a distinct active request fixed it; follow-up `4c868ca7-974b-4b3f-9b51-9023bd234863` confirmed the fix. Later zero-budget and close-classification mismatches were reproduced/fixed; two cancellation fixture hangs were corrected to wait for socket closure. Final **3,679 tests / 12.10s**, all-component build **14.844s**, full tooling/audits/reference integrity, eleven source checks and exact compiled-only example pass. Final coverage: 25 readiness cases plus RPC first-cause checks. No reviewer execution/sentinel attestation; later cause/zero work was self-reviewed/native-tested. Fess: `docs/development.md#connection-readiness-delivery`; receipts `/tmp/droid-readiness.3EmfuPgJ/`. Relay **4/5**, native-host **4/5**, dashboard **13/45**; hooks remain required.
- Completed retry/reconnect leaf: `.1.2.3`, started **2026-09-11 01:59:54 UTC**, closed/verified offline. `Factory.Droid.Retry` supplies bounded profiles, both jitter ranges, hooks/custom delays, explicit recovery/abort and delegated acquisition. Publication precedes caller work; body, cleanup and success-hook failures cannot restart acquisition. Factories must perform only replay-safe setup/auth before callback; session init/load/work belongs after publication. First build **02:22:15** met thirty minutes; initial mapping exceeded fifteen. Five ambiguous counters and a reviewed fixture scheduling assumption were fixed; held-worker evidence retained expected 1/got 0. Historical boundary: eighteen checks (**0.04s**), **3,654 tests / 11.12s**, build **5.385s**, tooling/audits/source checks and compiled example. No reviewer execution/sentinel attestation. Fess: `docs/development.md#connection-retry-delivery`; receipts `/tmp/droid-connection-retry.YxVNKDxz/`. At that boundary relay was **3/5**; use readiness's current boundary above.
- Completed binary-tunnel leaf: `.1.2.2`, started **2026-09-11 00:48:18 UTC**, closed/verified offline. Raw `MessageWebSocket`, full close metadata and shared explicit/scoped cleanup preserve old object APIs; relay authentication/prefix retirement are reused for exact binary data and encoded targets. First check began **01:04:48** and passed 46 existing cases; fourteen new tunnel cases pass with them (**60 tests / 1.20s**). Historical boundary: all-component build **10.585s**, **3,636 tests / 11.28s**, full tooling/audits/reference integrity, eight tunnel-class plus eight shared-auth source checks and matching compiled-only example. The failed concurrent-close fixture auto-ACKed through `receiveDataMessage`; cached source justified raw `WS.receive`, which now holds ACK while retaining wait/cancellation assertions. Static reviewer `8e74526e-9fb6-4300-be9b-94a418ddb44c` completed with no issues after supervisor coordination; the workflow wrapper lost continuation, but the child report is retained. No reviewer execution or sentinel attestation. Fess: `docs/development.md#binary-relay-tunnel-delivery`; receipts `/tmp/droid-relay-tunnel.AmUXcqS3/`. At that boundary relay was **2/5**; use the current retry boundary above.
- Completed relay-auth leaf: `.1.2.1`, started **2026-09-10 23:21:42 UTC**, closed/verified offline. `Transport.Relay` uses the existing WebSocket owner and daemon constructors; token providers refresh at auth/init/load without cached fallback, with per-scope organization and explicit API-key/bearer/grant separation. First build began **23:39:58 UTC**. Twenty-one relay plus twelve injected cases pass; historical boundary **3,622 tests (10.93s)**, all-component build **4.392s**, full tooling/audits/reference integrity, eight source checks and matching compiled-only example pass. A failed-send/buffered-prefix lifetime defect was reproduced and fixed; its first guard fix required an explicit polymorphic signature. Secondary review reported no issue before that later self-reviewed/native-tested fix; no independent attestation or reviewer execution. Full fess: `docs/development.md#relay-authentication-delivery`; receipts `/tmp/droid-relay-auth.IrkVR9dq/`. At that boundary relay was **1/5**; use the current binary-tunnel boundary above.
- Completed injection leaf: `.1.1.5`, started 22:07:25 UTC and closed/verified offline. `ObjectTransport`, local session-only options and daemon client options reuse existing constructors, without dummy endpoints. Inherited authentication explicitly takes identity/token; locality only governs hosted MCP. UUID namespaces prevent deterministic request-ID reuse. Per-RPC readiness settles before terminal/controller restoration, separately from load-generation publication. Twelve new cases; first check 22:26:55; full **3,601 tests (11.35s)**, build/full format/lint, five audits/reference integrity, five source checks and matching compiled-only example pass. Unused test import and formatting failures were fixed. Initial review hit its tool budget; follow-up completed constructor/generation checks with no findings. No reviewer execution or sentinel-attested independence. Full fess: `docs/development.md#injected-session-runtime-delivery`; receipts `/tmp/droid-injected.9OuRhf1P/`.
- Completed in-process leaf: `.1.1.4`, started 20:54:43 UTC and closed/verified offline. `Internal.MessageChannel` now owns shared callback framing/lifetime with declared send-failure policy; IPC public API is preserved. `Transport.InProcess` supplies lifecycle/events, full close data, nonterminal raw errors and delegated STM readiness gates. Fifteen new cases plus all seventeen IPC regressions pass; full **3,589 tests (11.72s)**, build/full format/lint, five audits/reference integrity, five source checks and matching compiled-only example pass. First meaningful build: 21:18:20. Full fess: `docs/development.md#in-process-channel-delivery`; receipts `/tmp/droid-inprocess.JbDq2dUC/`.
- In-process review corrections: early setup skipped runtime cleanup and the first test expected that wrong zero count; corrected expectations reproduced the P1. Teardown preceded concurrent unsubscribe, including cancellation. Shared `finishChannel` now joins before runtime disconnect; only lock acquisition is uninterruptible, callbacks remain interruptible, and `allowInterrupt` after unlock preserves deferred cancellation. The wait-only attempt's `Scope cancellation lost` receipt remains. Final narrow review confirmed the fixes; no sentinel-attested independence or reviewer execution claim.
- Completed supplied-IPC leaf: `hsdk-docs-package-signoff-ip3.1.1.3` (started 19:58:44 UTC, closed/verified offline). `Transport.IPC` bridges host text callbacks/availability/disconnect to object I/O for the existing RPC reader, owning only its subscriptions. Seventeen focused checks pass; full **3,574 tests (11.52s)**, build/full format/lint, five audits/reference integrity, five source checks and matching compiled-only example pass. The initial fixture omitted required `type: request`; it was corrected without codec changes. Secondary review found a real flush/failure-publication race, reproduced naturally and fixed by atomic retirement; narrow follow-up confirmed it. No sentinel-attested independence. Full fess: `docs/development.md#host-supplied-ipc-delivery`; receipts `/tmp/droid-host-channel.e684ytLH/`. Initial design exceeded its fifteen-minute target; first meaningful build at 20:23:00 was within thirty minutes. Normal-session injection remains `.5`; owned child-FD allocation remains `.2`.
- Current open leaf: launch/trusted-environment controls `hsdk-docs-package-signoff-ip3.1.1.2`, started 19:07:44 UTC. Prefix/complete replacement/extra argv, inherited→ordinary→sanitizer→trusted environment ordering, normal-session integration and explicit stderr routing through the existing cleanup owner are verified. Six new cases; first check 19:27:48; 216 focused tests (8.05s); full **3,557 tests (11.20s)**; build/full format/lint, five audits/reference integrity, four source checks and matching compiled-only example pass. Checkpoint fess: `docs/development.md#launch-configuration-checkpoint`; receipts `/tmp/droid-launch-config.dQ5wq7R5/`. This is not closure.
- Remaining constraint: no child IPC-enable flag is exposed. Installed `process-1.6.26.1` lacks selective extra-FD actions; its communication child endpoint is inheritable. Read-only advisory `161d9e82-4fa5-42ce-801f-c65862ce2d5f` / child `44f930e2-64d3-44eb-a53f-5af0676f72d9` proposed native descriptor actions plus experimental PID adoption, but atomic/private channel creation under the local macOS headers is unresolved. No adapter was implemented; no independently attested or live IPC claim. Keep `.2` open; do not replace the missing capability with an ignored flag or unsafe FD inheritance.
- Completed ACP leaf: `hsdk-docs-package-signoff-ip3.1.1.1` (started 18:33:50 UTC, closed/verified offline). Pure `DroidProcessMode`/`droidProcess` selects exact ACP or StreamJsonRpc argv and reuses existing JSONL/process ownership; ordinary Factory sessions stay StreamJsonRpc. Six new cases; 210 focused tests (7.56s); full **3,551 tests (10.54s)**; build/format/lint, five audits/reference integrity, two source checks and matching compiled-only example pass. First meaningful build began 18:44:23; assertion-precedence/format errors were corrected. Full fess and receipts: `docs/development.md#acp-process-mode-delivery`, `/tmp/droid-acp-mode.Ndy18UiX/`.
- Verification correction: the first ACP full suite exposed the earlier pending-conflict fixture assuming plain RPC return implied intake publication. It now waits for a later ordered cwd marker; the error and wire assertions and production behavior are unchanged. Focused test, 20 repetitions and final full suite pass. The failed `suite-final.log` and corrected `suite-verified.log` remain distinct; the old closed issue has appended follow-through evidence. Do not describe the earlier passing run as proof against every schedule.
- Completed child: message/progress state (`hsdk-daemon-resources-observability-mxt.4.4`, closed/verified offline). Conversation/todo/working/progress/hook state, checked views, progressive expansion, pruning and hydration-floor selection are accepted. Final full suite: **3,435 tests, 9.93s**; build/format/lint/audits and compiled selection example pass. Native progress expiry passed at **60.004025s** and hook lease expiry at **90.000039s**, without inventing a completion report. Receipts: `/tmp/droid-message-state.d9ZShJ/state-*`, `hooks-display-*`, `hook-lease-runtime.log`; full combined fess: `docs/development.md#session-message-state-closure`.
- Completed child: queue coordination (`hsdk-daemon-resources-observability-mxt.4.5`, closed/verified offline). Ordered queue transitions, processed-ID guards, attachment-aware pause, accepted-load restoration, explicit sending and ACK-aware resolution are accepted. A cancelled-send restoration race was reproduced and fixed: newer queue observations now win. Twenty new queue cases and the fresh **3,455-test suite (9.82s)** pass. No implicit replay or text/time delivery inference. Receipts: `/tmp/droid-queue-state.2pZQPh/`; full fess: `docs/development.md#queue-state-delivery`.
- Completed child: attachment/hydration (`hsdk-daemon-resources-observability-mxt.4.6`, closed/verified offline). Typed linkage/summaries, ordered discovery, optional automatic hydration and explicit ensure reuse existing owners. Silent-close/stale-load and first-registration lookup regressions were reproduced and fixed. **22 child cases; 3,477 tests in 10.55s** at that boundary. Receipts: `/tmp/droid-child-state.bap0r5/`; full fess: `docs/development.md#child-session-hydration-delivery`. Parent-cwd seeding stays with `.4.8`.
- Completed child: terminal restoration (`hsdk-daemon-resources-observability-mxt.4.7`, closed/verified offline). Retained metadata/selection, serialized state, buffers, scoped sinks and default automatic restoration use existing owners. Publication, missing-screen data loss and writer-error visibility regressions were reproduced and fixed. **24 new cases; 3,501 tests in 10.52s** at that boundary. Older peers serve the new query explicitly; no defaults or tests were disabled. Receipts: `/tmp/droid-terminal-state.jeOkre/`; full fess: `docs/development.md#terminal-restoration-delivery`. Timestamp coercion remains `hsdk-9j2`.
- Completed child: authoritative cwd (`hsdk-daemon-resources-observability-mxt.4.8`, closed/verified offline). Init/load precedence, resolved changes, ordered cwd events, invalid observations and provisional parent inheritance use existing owners. The public pure-reducer omission was reproduced and fixed. **12 new cases; 3,513 tests in 10.13s** at that boundary. Receipts: `/tmp/droid-cwd-state.7fcRMc/`; full fess: `docs/development.md#authoritative-cwd-delivery`. No path normalization, trust inference or process-cwd mutation was added.
- Completed child: pending/deferred interactions (`.4.9`, closed/verified offline). Enumeration/waits/subscriptions/external fanout, request-local tokens/choice, inactive/deferred decisions, mutable handlers, logout/auth clearing and prepared admission use existing owners. **32 new cases; 3,545 tests in 10.32s**; build/full format/lint, five audits/reference integrity, eight source checks and matching compiled-only example pass. Four review findings were reproduced and fixed; the reviewer confirmed the original three, while the fourth correction and final association coverage are native-tested/self-reviewed. No sentinel-attested independence claim. Full fess: `docs/development.md#pending-interaction-delivery`; receipts `/tmp/droid-pending-interactions.8HFhXY/*-verified.log` and preserved failures.
- Parent acceptance: all nine state children and all 35 mandatory resource descendants were queried and verified closed; sixteen historical direct resource slices remain closed. Actual family receipts and current test registration were checked against the shared 3,545-test result. An administrative test-name assertion was corrected from the actual `test/Main.hs` imports, without changing a test. Proofs: `parent-integration.log`, `resource-parent-integration.json`; full parent fess: `docs/development.md#coordinated-state-and-resource-parent-acceptance`.
- Historical `.4.4` mapping exceeded the lookup target; its first meaningful `-Werror` build began at 04:49:55 UTC, within thirty minutes. The compile/format failures and deliberate UTF-16 ordering difference remain recorded in its delivery notes. Do not repeat that investigation as the current task.

## Short restart handoff

**The issue-tracking revision is materialized.** There are 102 linked Obr issues: 86 newly created and 16 reused, comprising 39 issue-linked dashboard rows and 63 detailed children. The 45-row flat dashboard also retains six completed historical capability/audit rows. See the [goal-to-Obr crosswalk](parity.md#goal-to-obr-crosswalk); the full hierarchy and acceptance criteria live in `PLAN.org`/Obr. These counts describe tracking granularity, not newly delivered SDK capabilities.

- The **50–95-hour estimate is withdrawn** as a forecast, test-runtime estimate or approval gate. No time/scope decision is required to accept that number. The closure audit must not repeat.
- Latest integrations: **3,941 tests** on GHC9.12.4, macOS ARM64/**29.87s** and GNU/Linux ARM64/**61.58s**. Build, formatting/lint, reference audits and all77 README snippets pass. The newly approved local CLI0.217.0 one-shot check passes with observed cleanup. Existing source archives remain historical; no current independent archive claim.
- **Release certification remains incomplete; dashboard36/45.** Compiler and observed ARM64 Mac/Linux verification are accepted. The one local approval is consumed, not a blanket grant for more prompts or unspecified daemon scenarios. The dependency-URL defect is fixed; remaining internal/instance documentation links, broader live scenarios and final package/distribution checks stay open. The missing repository URL is an advisory, not an SDK failure or whole-goal pause.
- Timestamp decision is resolved: user selected strict wire strings over active TS Date coercion. The retained23-case/two-timezone reference observations remain in `terminal-timestamps-20260912/`; new native checks are in `contract-policy-20260913/`. Current scalar preserves spelling and rejects nonstrings/calendar rollover. Its documented leap-position validation is not IERS occurrence certification; that distinction is tested rather than hidden. Do not ask the same policy question again.
- Initial timestamp investigation exceeded the fifteen-minute ceiling while establishing the conflicting active decoder and schema; the bounded reference probe and baseline ran before thirty minutes. PAL was connected, but its restriction policy permits neither required GPT nor Gemini advisor; no substitute Factory/model call occurred. Owned IPC and file timestamps are now verified on macOS/Linux ARM64, without granting new image/live/paid/account/commit/publication permission.
- Remaining evidence concerns broader platform coverage, documentation diagnostics, final package/distribution checks and defined additional live scenarios. Timestamp/attribution, validator/licensing and the stable compiler decision are resolved. GHC9.14 and an invented repository URL are not release prerequisites. No publication or new live call is implied by checkpoint continuation.

Read the detailed sections below only when the selected capability requires them; do not reconstruct the old conversation as startup work.

The complete remaining-scope report is outside the repository, as requested: `~/dl/droid-sdk-haskell-remaining-scope-2026-09-08.md`. On this machine `~/dl` resolves to `~/Downloads`. Stopping-point versions of this handoff, the issue tracker and selected evidence are committed. Resumed development and its newer records remain uncommitted and are not covered by the stopping-point recovery bundle.

**Push remains unresolved:** no Git remote was configured and no destination URL was supplied. Do not guess a repository owner, create a repository or select its visibility. Obtain the destination before pushing. Local commits and a local bundle are not an off-machine backup.

## What is implemented

| Milestone | Delivered scope | Status |
| --- | --- | --- |
| `haskell-foundation` | Cabal/GHC2024, Nix shell, CI configuration, licenses, immutable schemas, reference audit and codec foundation | Complete foundation; other-platform execution remains separate |
| `sessions-streams` | Local new/resumed sessions, one-shot/follow-up turns, complete/partial events, outcomes, interruption, model/context queries, fork/compact/rewind retirement and rollback, subscriptions and ordered settings observations | Complete local core |
| `inputs-controls` | Image/text/PDF attachments, raw/typed structured output, safe permission/question handling, settings, initialization-only prompts, tool/skill/command controls and hook events | Complete local core |
| `daemon-session` | Native plain/TLS WebSockets, explicit authentication, create/load, shared turns/events/inputs/output, pending interactions, interruption and owned cleanup | Complete offline vertical slice |
| `mcp-external` | Local/daemon startup configuration, reports/registry, add/remove/toggle/auth operations, early events, configuration replay and sessionless global daemon get/update | Complete offline scope |
| `mcp-hosted` | Raw/typed/structured Haskell tools, authenticated loopback HTTP, exact native input/output validation, rich results, limits, cancellation and manual/scoped ownership | Complete offline scope, including validator deployment/licensing; platform/live gates remain separate |
| `daemon-resources` | REST, daemon resource families, cron/Software Factory, mission association and all nine coordinated-state children through existing ownership | Complete approved resource/state scope offline; advanced transports, observability and other sibling/release gates remain required |

The library runs natively: Python is used only for development audits, and Node.js is not required. Existing CLI/daemon processes remain the agent engine. The main facade is `Factory.Droid`; private `Internal.Session` owns shared lifecycle behavior, `Internal.Stream` owns reduction, and `Protocol`/`Protocol.Dispatch` own correlation and server-request admission. Do not introduce a competing session or callback runtime.

## Delivery procedure after explicit resume

1. Re-read the refocus skill/state and confirmed goal, check current Git status and select the corresponding goal/Obr capability. Do not reconstruct history or repeat the closure audit.
2. Use the existing verified hierarchy/crosswalk; do not recreate issues or reopen delivered defaults/mission association. Select the next ready capability and keep its goal focus and Obr children current.
3. Complete distinct behavioral subtasks through existing abstractions with focused checks; broad checks belong at coherent/shared-runtime boundaries. Do not create codec/export-count targets.
4. Surface genuine authorization, licensing and platform gates while continuing correctness-independent work. No silent matrix reduction, validator downgrade or unsupported attribution.
5. Report actual accepted capabilities, issue IDs, checks/results and next ready work. No unsupported forecast gate, repeated roadmap or redundant test cycle.
6. **At the end of every subtask, run the `fess` skill before marking it complete or committing.** Invoke `/fess`, or load `command-fess/SKILL.md` and execute its full rubric. The current installation is `/Users/johnw/.agents/skills/command-fess/SKILL.md`. Record findings, verification gaps, scope drift and the next fix; do not replace the audit with an assertion that tests pass. If the skill is unavailable, obtain it before closing the subtask. Delegated independence requires the skill's explicit no-history attestation and sentinel probe.

## Remaining roadmap

This is functional scope, not an exhaustive schema-generation queue.

1. **Hosted conformance and provenance:** required JSON Schema behavior, advertised versus enforced constraints, argument/output/error semantics, compiler bounds, and `jsonschema` license clarification. Keep HTTP authentication, bounded envelopes, argument-only handlers and cancellation ownership intact.
2. **Resource/state follow-through:** the approved parent and its 35 required descendants are closed offline. Broader create/resume option mapping and terminal timestamp conformance (`hsdk-9j2`) remain required sibling work, not waived by closure. Newer schema-only methods do not expand the baselined capability scope; preserve the implemented resource/state APIs.
3. **Advanced transports:** required ACP/process modes, relay/binary tunnels, host-supplied message channels and exposed low-level facilities. Reuse existing framing/correlation/lifecycle primitives; test authentication, cancellation, malformed frames, disconnect and ownership with native peers.
4. **Discovery and remaining configuration:** saved-session filesystem discovery (`hsdk-f20`) and exact mapping of missing create/resume options, standalone/legacy stream feeds and functional helpers. Reuse delivered daemon settings, skills, fork/compact/rewind, queue and message-selection behavior rather than assuming it is missing. Daemon replacement returns successor identifiers without retiring the source, unlike local replacement.
5. **Attribution, observability and compatibility:** honest Haskell identity; trace/log/metric propagation and privacy; explicit reconciliation of SDK/CLI/schema conflicts; required runtime refinements, diagnostic text and timestamp semantics.
6. **Conformance and platform execution:** complete capability matrix, native/reference-derived failure coverage, actual macOS/Linux results on the declared compiler matrix, and freshly authorized bounded CLI/daemon interoperability tests. CI configuration is not execution evidence.
7. **Distribution sign-off:** documentation/migration/examples, Haddock links, source-repository metadata, licensing, independent source distribution and final scope/audit reconciliation. No required omission, stub or unapproved reduction may remain.

### Current tracker anchors

- `hsdk-mcp-y1i`, `hsdk-zpf`, `hsdk-4po`: closed/verified offline; external MCP history preserved and hosted validation, licensing and SDK integration accepted.
- `hsdk-docs-package-signoff-ip3.1`, `.2`, `.3`: remaining owned child IPC, Linux saved-session timestamps and attribution/protocol decisions. Resource/state, relay, configuration, helpers and observability parents are closed.
- `hsdk-f20`: scanning and selection closed; macOS timestamp behavior implemented, Linux execution still required.
- `hsdk-types-codecs-2rb`: required operation coverage, not exhaustive schemas; the baseline inventory `hsdk-reference-baseline-h1v` is already closed.
- `hsdk-haskell-attribution-80m`, `hsdk-protocol-baselines-wvf`, `hsdk-runtime-refinements-tfo`: material contract decisions.
- `hsdk-w8y`: timestamp occurrence conformance where required by operations. Diagnostic source-length handling is resolved separately at submission; canonical codecs remain unchanged.
- `hsdk-conformance-integration-m3p`, `hsdk-docs-package-signoff-ip3`, `hsdk-documentation-diagnostics-jvc`: final verification/documentation.
- `hsdk-org-export-whitespace-x4c`: Obr's trailing blank-line exporter defect.
- `hsdk-vtk`: deferred optional upstream WebSocket reporting, not missing local safeguards.

## Invariants and known limits

- Preserve asynchronous exception identity and masked ownership transitions. Uncertain mutations invalidate before releasing their lease. Atomic retirement, not return-value receipt, commits a local replacement.
- Intake prefix barriers are not sleeps, snapshot epochs or worker joins. Typed known events fail on malformed payloads; unknown events remain explicit raw data. Delivered text prefixes and authoritative final buffers are distinct.
- Permissions/questions default to cancel/reject. A configured permission handler or explicit daemon manual-response scope changes permission policy. Permission associations and execution-only question authority remain isolated; local response acceptance is not remote ACK. This policy is not a sandbox.
- Daemon scopes own connections, not the external daemon or saved-session storage. Normal exit does not kill the daemon, log out or delete/close saved sessions.
- TLS verification is the default. Plaintext/private roots are explicit. Authentication is protocol-level; no inferred endpoint suffix or browser/OAuth exchange is provided.
- MCP acknowledgements are not authentication completion. Early MCP observers are connection-scoped, before init/load, and remain through replacement/rollback. Configuration preserves omission/empty/false and header-array versus map distinctions.
- Hosted servers bind IPv4 loopback with a random bearer. Defaults: 4 MiB request, 10 MiB response, thirty-second tool deadline. Manual starts pin ownership; leases borrow/share it; restart rotates the token. Cleanup joins handlers and therefore requires cooperative cancellation. Client disconnect does not undo side effects or promise cross-POST cancellation.
- Hosted schemas reject unsupported patterns, unevaluated/dynamic/external references and ambiguous `~01` escapes. `$ref` siblings are normalized conjunctively. Format/content fields are annotations. These restrictions remain a parity gap, not an approved subset.
- Attachment limits are 5 MiB for image/text and 3 MiB for PDF; signature checks are not full document parsers. Turn-output adaptation is not full JSON Schema validation. Redacted `Show` does not sanitize explicit fields or encoded JSON.
- Saved-session discovery hit macOS `getExtendedFileStatus: unsupported operation (Operation is not supported)`. No discovery module or birth-time binding was delivered. Do not silently substitute a different timestamp contract.

## Verification and preserved evidence

Selected stopping-point logs, reviews, reference information and an archived goal snapshot reside in `docs/evidence/2026-09-08/`; its README distinguishes those checks from historical evidence. Its `source-sha256.txt` identifies the stopping-point implementation, not later uncommitted changes. Read current Git changes and the newer development notes before comparing snapshots. Original temporary paths in older notes are optional investigation aids, not required recovery inputs.

**Historical baseline before the default-settings edits:** raw-tool output schemas, REST, catalogs, workspace/files, terminals and worktree initialization passed warning-free builds and all 3,167 tests. Catalog coverage includes thirteen schema/native cases and read-only source review; workspace adds six native cases including binary transfers by a mock daemon; terminal adds six protocol/lifecycle cases; worktree adds seven native and ten schema/codec cases. These do not execute a real PTY/Git worktree or implement state managers. Focused Ormolu/HLint, reference audits and compiled README examples passed for those snapshots. The earlier REST checkpoint additionally passed actionlint, Haddock with link warnings, Cabal check with `[no-repository]`, and an independent 110-file source distribution with 3,125 tests and all six attachment probes. Those package receipts predate the later binding increments; full release cycles were not repeated. No SDK build or test was run during this execution-plan revision.

Hosted validation remains unresolved. jsoncons 1.9.0 failed precision/regex cases. Rust 0.55.1 passed twelve fixture pairs only after correcting serde reserved-key type confusion, then failed a direct-FFI cancellation check on a small valid schema. No alternative was adopted or existing guard removed. See `docs/development.md` for exact cases, versions, hashes and evidence paths.

Fresh stopping-point checks established:

- Local snapshot: warning-free build and 3,005 passing tests.
- Daemon/external-MCP snapshot: recorded fingerprints match, warning-free build and 3,092 passing tests.
- Hosted implementation: warning-free build and 3,110 passing tests; Ormolu, HLint and actionlint pass.
- Hosted README example matches its compiled source and compiles without execution. Five reference-audit tests pass.
- Haddock completes with link warnings; Cabal check retains `[no-repository]`. Initial offline Haddock lacked source archives; the download attempt exposed a missing matching `zlib.h`. Installing the corresponding Nix development output and setting the include path resolved that build failure.
- The unpacked hosted distribution builds and passes 3,110 tests. Its attachment probe exposed the Base64 package-name collision; commit `d6f86ce` switches the probe to the library's Cabal REPL. Root probes pass all six behavioral markers. See the evidence README for the final distribution recheck.

One earlier authorized local run streamed `HELLO`, matched the final result and reaped its child; it predates later changes. Two prompt attempts were consumed, the first failing before optional local-session routing was corrected. No current live daemon, two-turn, rich-event, hosted-MCP or OAuth interoperability claim is made.

Only Apple Silicon macOS/GHC 9.10.3 execution is established. GHC 9.12.4/9.14.1 and Linux CI entries are proposed execution targets, not a completed support matrix. The current validator's base bound excludes the proposed GHC 9.14 line. Resolve this explicitly; do not suppress the job or call it verified.

## Resume on this machine

```sh
cd /Users/johnw/work/positron/droid-sdk-haskell
git status --short
git log -7 --oneline
obr ready
obr show hsdk-daemon-resources-observability-mxt.4.9
```

Current Wiggum policy requires `direnv exec .` for project tools and `de` for environment regeneration. Do not run build commands through `nix develop` or use hard-coded tool paths. The cache is currently blocked as recorded above. The following is historical diagnostic evidence only, not the current execution procedure:

```sh
export PATH="/nix/store/y3giz0m11fsc55jl2xbk2jpzyinm48ar-ghc-9.10.3-with-packages/bin:/nix/store/ccjihgs38m5qgvksik031vjz1cn9qksy-cabal-install-3.16.1.0/bin:/nix/store/8d4a6sf2k7bdjjaw21h9h0ggvvnhv8cm-hlint-3.10/bin:$PATH"
export NIX_LDFLAGS_arm64_apple_darwin="-L/nix/store/008518i9kpvzsvs6d9rgcwgicjp5l4zb-zlib-1.3.2/lib"
export NIX_CFLAGS_COMPILE_arm64_apple_darwin="-I/nix/store/k2sq5ihmlgs9vv5j74jr3bqywvj2jsxr-zlib-1.3.2-dev/include"
cabal build all --offline --ghc-options=-Werror
cabal test all --offline --ghc-options=-Werror --test-show-details=direct \
  --test-option=--hide-successes --test-option=-j1 \
  --test-option=+RTS --test-option=-N2 --test-option=-RTS
python3 -B scripts/check_attachment_files.py
python3 -B -m unittest discover -s scripts -p 'test_*.py' -v
python3 -B scripts/reference_schemas.py
find src test examples -name '*.hs' -exec ormolu --mode check {} +
hlint src test examples +RTS -N1 -RTS
actionlint .github/workflows/ci.yml
```

These store paths are machine-specific, not an installation contract. Missing public dependency archives require an ordinary Cabal download rather than fabricated success from `--offline`. Run only one Cabal writer at a time. Do not run `droid-example`, README model examples or the archived live driver without new authorization.

On a future restart, check live goal state once and follow explicit user lifecycle instructions; historical pause snapshots do not override resumption. Maintain the existing task tree and continue the next incomplete capability, not a new survey of completed work. The downstream `fess` gate applies to each subtask.

## Cold-machine recovery

1. Obtain the pushed repository, or transfer the local bundle `~/dl/droid-sdk-haskell-2026-09-08.bundle` to the new machine and clone it. Until transfer or push is confirmed, no off-machine backup exists.
2. Read this handoff and `docs/evidence/2026-09-08/goal-snapshot.md`. The latter is an archive, not a file to execute or an instruction to resume automatically. If Pi goal state is absent, have the user explicitly recreate the objective and restore the twelve-task plan from that snapshot with confirmation. Preserve the five completed scopes and seven remaining ones.
3. Rebuild Obr's local cache from tracked `PLAN.org`: `obr init && obr sync --import-only --rebuild`. Never copy or commit `.obr/` as the authoritative state.
4. Install Nix or the documented native GHC/Cabal prerequisites; enter the checked-in shell or reproduce the declared dependency matrix. Re-run the baseline on the new platform and retain the result. Do not copy ARM64 Nix paths onto Linux.
5. Recover reference sources as needed: Python `https://github.com/Factory-AI/droid-sdk-python` at `6d06b4e613aab3990cf2ced469b5c4e80053e52e`; TypeScript guides at `https://github.com/Factory-AI/droid-sdk-typescript`, commit `87b4c4c4e4fc093e10a94f1e767ed57b7cbf597e`. Published package URLs/integrities are in the evidence README and parity document. Verify revisions and hashes; do not follow current upstream heads.
6. The four supplied schema files and their fingerprints are already committed. The old Desktop/Downloads input directory is not required. Reacquire the selected CLI only for explicitly authorized live work; prior static offsets apply solely to its recorded binary hash.

A suitable fresh-session instruction is: “Read the short restart handoff in `docs/HANDOFF.md` and load existing goal `mtnsrj8c-uuc099`. Preserve all work and milestones. Follow live goal state and my explicit resume instructions; continue the next incomplete capability under the recorded investigation limits. Default-settings verification is already delivered. Report completed capabilities rather than test totals; run `fess` at each subtask boundary. No live calls, outreach, commits or publication without fresh authorization.”
