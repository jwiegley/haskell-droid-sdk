-- | Scoped local Droid sessions. The shared runtime owns RPC correlation,
-- callbacks, turns and cleanup; see Factory.Droid.Daemon for existing daemons.
module Factory.Droid
  ( DroidOptions (..),
    defaultDroidOptions,
    DroidSessionOptions (..),
    defaultDroidSessionOptions,
    droidSessionOptions,
    withDroidSessionOn,
    withDroidSessionOnHandlers,
    withResumedDroidSessionOn,
    withResumedDroidSessionOnHandlers,
    DroidSession,
    DroidSessionStatus (..),
    droidSessionStatus,
    onDroidSessionEvent,
    onDroidMissionSnapshot,
    DroidReplacementError (..),
    DroidRewindOptions (..),
    droidSessionId,
    DroidResult (..),
    DroidEvent (..),
    DroidStreamMode (..),
    DroidInput (..),
    droidInput,
    DroidOutput,
    DroidOutputError (..),
    DroidOutputResult (..),
    rawDroidOutput,
    jsonDroidOutput,
    SystemPromptConfig,
    customSystemPrompt,
    appendedSystemPrompt,
    DroidError (..),
    DroidHandlers (..),
    defaultDroidHandlers,
    DroidInteraction (..),
    DroidInteractionFailure (..),
    DroidMcpFailure (..),
    withDroidSession,
    withObservedDroidSession,
    withObservedDroidSessionOn,
    withResumedDroidSession,
    withDroidSessionHandlers,
    withResumedDroidSessionHandlers,
    sendPrompt,
    sendDroidTurn,
    sendDroidEvents,
    sendDroidOutput,
    sendDroidOutputEvents,
    sendDroidInput,
    sendDroidInputEvents,
    sendDroidInputOutput,
    sendDroidInputOutputEvents,
    interruptDroidSession,
    runDroid,
    streamDroid,
    DroidSessionFile (..),
    DroidSavedSession (..),
    ListDroidSessionsOptions (..),
    defaultListDroidSessionsOptions,
    listDroidSessions,
    sessionFileCreatedAt,
    listDroidModels,
    listDroidTools,
    listDroidCommands,
    listDroidSkills,
    listDroidMcpServers,
    listDroidMcpTools,
    listDroidMcpRegistry,
    addDroidMcpServer,
    removeDroidMcpServer,
    toggleDroidMcpServer,
    toggleDroidMcpTool,
    authenticateDroidMcpServer,
    cancelDroidMcpAuth,
    clearDroidMcpAuth,
    submitDroidMcpAuthCode,
    submitDroidMcpAuthError,
    getDroidSettings,
    getDroidMissionSnapshot,
    updateDroidSettings,
    setDroidSkillDisabled,
    getDroidContextStats,
    getDroidContextBreakdown,
    renameDroidSession,
    changeDroidWorkingDirectory,
    getDroidWorkingDirectory,
    getDroidWorkingDirectoryState,
    getDroidRewindInfo,
    forkDroidSession,
    compactDroidSession,
    rewindDroidSession,
  )
where

import Factory.Droid.Discovery (DroidSavedSession (..), DroidSessionFile (..), ListDroidSessionsOptions (..), defaultListDroidSessionsOptions, listDroidSessions, sessionFileCreatedAt)
import Factory.Droid.Internal.Session
